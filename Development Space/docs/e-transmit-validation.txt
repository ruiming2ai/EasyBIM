EasyBIM e-transmit 2.1.5 verification
Date: 2026-09-25

Executed checks (filesystem and API-shaped tests, not Autodesk Revit):

1. Linux / CPython 3.10.6
   python3 -B -m unittest discover -s "Development Space/tests/etransmit"
   Ran 365 tests. OK, 4 platform skips. No failures or errors.

2. Windows / bundled CPython 3.12.14
   python.exe -B -m unittest discover -s "Development Space/tests/etransmit"
   Ran 365 tests. OK, 2 skips. No failures or errors.
   Executed against a copied source/test snapshot on the Windows filesystem.
   Long-path package, report, batch index, ZIP and cleanup tests ran on Windows.

3. Windows / official IronPython 2.7.12, net45 runtime
   Same selection as repository CI: test_21*.py plus runtime_copy,
   runtime_progress, source_automation, payload_acquisition, runtime_longpaths,
   runtime_reference_repairs and 209_regressions.
   Ran 259 tests. OK, 1 platform skip. No failures or errors.
   Initial attempts exposed Python 2 unittest fixture-binding/default-method
   incompatibilities in the new tests. Corrected the fixtures; full rerun passed.
   All 13 new cases also passed again under Linux and Windows CPython after
   those fixture-only corrections.

4. git diff --check: passed.
   Native helper source and shipped DLL are unchanged; no new native build was
   required. The 2.1.4 build/checksum result is historical, not a new 2.1.5 run.

13 added regression cases cover the uploaded Morrison revision metadata with
same-edition/different-byte cache files, direct/LinkedModels role selection,
discovery-order independence, exact-revision precedence, conflicting revisions,
formats, accounts, roots and same-role variants, unknown layouts, linked-only
availability, retained snapshot tampering, readable failed-host diagnostics,
and snapshot reuse across repeated/nested links back to the host in all layouts.
The existing layout, cache stability/cancellation, saved dependency inventory,
source integrity, load-state, rollback, delivery, ZIP and no-duplicate regressions
remain in the passing suites.

Model surrogates and API-shaped fixtures cannot prove actual RVT portability.
Not executed: Revit 2024 exports of Morrison/Anthropology in all layouts, WPF
rendering, Desktop Connector hydration, actual RVT detach/open/repath, relocation,
linked tags/dimensions and model placement. No Revit installation or company ACC
cache is present on this development machine. Complete
 e-transmit-desktop-checklist.md on the Revit workstation.

Review: local code review and regression tests. No independent agent review.
