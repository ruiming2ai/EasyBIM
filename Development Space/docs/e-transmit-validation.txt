EasyBIM e-transmit 2.1.8 verification
Date: 2026-09-26
Baseline: main 8ea07d21a54b51900a1279d8e883d5be11d11c01.

Executed locally, Linux / CPython 3.13:
python -B -m unittest discover -s "Development Space/tests/etransmit"
400 tests; OK; 4 platform skips.
Each test_218*.py script also executed independently and passed.
git diff --check: passed.

Initial regression runs failed on duplicate linked-RVT processing/opening,
double native acquisition, absence of timing data, and indirect dependency
copying. Subsequent regressions caught unmeasured initial registry discovery,
unnecessary child scanning, and an outer batch profiler replacing the package
profile. They pass after the production corrections.

Six-RVT simulated comparison (real file IO; substituted Revit API boundaries):
2.1.7: 37 verified copies; 6 processing calls; 12 verification calls; 5 links copied.
2.1.8:  9 verified copies; 1 processing call; 1 verification call; 5 links copied.
Not a Revit wall-clock benchmark. Source/link integrity preserved in the fixture.

No native C# source or bundled DLL changes. No new dependencies or installer.
GitHub Actions performs the existing Windows, Linux and IronPython checks on
this revision; consult that commit's actual workflow result, not an older run.

Not executed locally: Windows/IronPython runtime, actual Revit 2024/2025,
real ACC cache acquisition, Desktop Connector hydration, linked annotations,
real placement, final image/cloud repathing, and relocation acceptance.
Portable/API-shaped tests do not establish actual Revit portability or speed.
See e-transmit-2.1.8.md for scope, timers and workstation acceptance.
