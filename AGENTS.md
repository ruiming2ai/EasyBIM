# AGENTS Guide

## Project Snapshot

- Project name: EasyBIM.extension
- Status: Active pyRevit extension development on `main`
- Primary goal: Maintain and expand EasyBIM pyRevit commands/hooks with safe workflow governance
- Primary stack: Python (pyRevit/Revit API), XAML for command UI

## Current Workflow Summary

- Work inside existing pyRevit folder conventions (`*.panel`, `*.pushbutton`, `*.pulldown`).
- Keep command scripts focused and consistent with existing import/transaction patterns.
- Validate Python syntax for changed scripts before handoff (`python -m py_compile <script.py>`).
- Update command icons/bundles only when behavior or UX changes require it.
- Keep repo-level workflow decisions documented in `AGENTS.md` and mirrored in the repo parent skill.
- Use shorthand command phrase `sync worktrees` for the standard parallel-worktree sync workflow.
- Default meaning of that phrase:
  - Commit + push on the active feature worktree branch first.
  - Identify the creator branch associated with that feature worktree. `Temp-Phase-and-View-2` is only an example creator branch, not a fixed target.
  - In primary worktree, sync the creator branch with the feature worktree branch.
  - Stop after the creator branch is updated unless a separate merge to `main` is explicitly requested.

## Commands

- Python syntax check for one file:
  - `python -m py_compile "<path-to-script.py>"`
- Compile many command scripts:
  - `python -m compileall "EasyBIM.tab"`
- Primary worktree and branch checks:
  - `git worktree list`
  - `git rev-parse --abbrev-ref HEAD`
- Standard creator-branch sync commands:
  - `git add <files>`
  - `git commit -m "<message>"`
  - `git push origin <feature-worktree-branch>`
  - `git checkout <creator-branch>` (in primary worktree)
  - `git pull origin <creator-branch>`
  - `git merge --no-ff origin/<feature-worktree-branch>`
  - `git push origin <creator-branch>`
- Optional later promotion to `main`:
  - `git checkout main`
  - `git pull origin main`
  - `git merge --no-ff <creator-branch>`
  - `git push origin main`

## Conventions

- Keep edits narrow and feature-scoped.
- Follow existing command naming and folder structure.
- Prefer shared helpers under `lib/` for repeated logic.
- Keep UI labels and command behavior aligned.

## Git Worktree Manager Style

- Naming: create worktree folders as `C:\Users\RML\Documents\GitHub\EasyBIM.extension-<BranchName>`.
- Explicit-name override: if user provides a specific worktree name/path, create that exact folder and create/check out a branch with the exact same name as that worktree folder name.
- One branch per worktree: never keep the same branch checked out in more than one worktree.
- Branch conflict handling:
  - If a requested branch already has a worktree, reuse that worktree instead of creating a duplicate.
  - If that branch must move into primary, remove the secondary worktree and switch primary to that branch.
- Do not auto-generate suffix branch names (for example `*-Worktree-1`) when user requests an explicit worktree name.
- Primary branch movement:
  - No fixed fallback branch is enforced.
  - Primary branch switching is situational and user-directed.
- Bulk-create defaults:
  - Target all local non-`main` branches by default.
  - Skip branches already represented by same-branch worktrees.
  - If target path exists but is not a registered worktree, classify it as `path conflict`, skip it, and report it.
- Deletion defaults:
  - Prefer `git worktree remove <path>` first.
  - Use `git worktree remove --force <path>` only when complete deletion is explicitly requested and local edits may exist.
  - If folder deletion fails because it is locked/in use, report it and provide a retry command after handles are closed.
  - Run `git worktree prune` after removals.
- Orphan handling:
  - If a folder exists on disk but is absent from `git worktree list`, treat it as an orphan worktree folder.
  - Remove orphan folders only when explicitly requested.

## Decision Log

| Date | Decision | Why | Impact | Status |
|------|----------|-----|--------|--------|
| 2026-08-20 | Circuit Schedule reads the distribution hierarchy from `ElectricalSystem.BaseEquipment` + `.Elements` only, never `MEPModel` | Those two are unchanged 2015-2027, while `MEPModel.ElectricalSystems` -> `GetElectricalSystems()` (~2017) and `GetAssignedElectricalSystems()` (2018+) drifted; deriving parent and child from one source also stops them ever disagreeing about who feeds whom | One collector pass yields every edge of the graph; no per-element API round trip | Active |
| 2026-08-20 | Circuit Schedule reads every circuit value as display text via `AsValueString()` | The browser is read-only, so no value travels back into the model | The whole 2021+ `ForgeTypeId` / legacy `DisplayUnitType` unit split that `circuit_rating_revit` carries is absent from this tool | Active |
| 2026-08-20 | Circuit Schedule's pane, state and Revit modules live in `lib/easybim/`, not the pushbutton folder | `RegisterDockablePane` is only accepted during Revit app init, so `startup.py` must import the pane - and a `.pushbutton` folder is only on `sys.path` while its own `script.py` runs | Same placement rule as `clash_detection_panel`; `collect_circuits` is duplicated across the boundary as a result, a candidate hoist if the overlap grows | Active |
| 2026-08-20 | Circuit Schedule defers model calls through `ExternalEventBridge`, not `idling` | The pane responds only to clicks; an Idling consumer would run this tool's code on every idle tick all session to service an occasional double-click | `script.py` creates the event in API context on every button press, so a pyRevit reload is recovered by clicking the button again | Active |
| 2026-08-20 | Circuit Schedule matches circuit numbers by token, everything else by substring | In a project with three-digit circuits, a substring search for `12` returns every `112` and `120` and number search becomes useless | `12` finds `12` and `12,14,16` but not `112`; panel and load names stay substring | Active |
| 2026-08-20 | Circuit Schedule filters by flipping `is_visible` on the existing nodes rather than rebuilding a pruned tree | Rebuilding hands WPF new objects on every keystroke and drops the selection, which is what the breadcrumb is drawn from | `apply_filter` stays a pure function over the node tree and is unit tested | Active |
| 2026-03-02 | Standardize repository workflow source on `AGENTS.md` | Align global hub + parent skill governance | Enables reliable drift checks and skill maintenance | Active |
| 2026-03-04 | Standardize Git worktree manager style rules | Preserve consistent branch/worktree behavior across sessions | Reduces duplicate checkouts, path conflict churn, and deletion errors | Active |
| 2026-03-04 | Adopt standard promotion pipeline (`branch -> primary worktree branch -> main`) | Ensure predictable delivery from worktrees to production branch | Reduces missed merges and branch drift | Active |
| 2026-03-04 | Standardize shorthand phrase `update feature & main worktree` | Reduce ambiguity and typing for routine promotion requests | Faster communication and fewer git wording mistakes | Superseded |
| 2026-03-04 | Enforce explicit-name worktree/branch parity | Match user-provided worktree names exactly and prevent unwanted auto-suffix branches | Eliminates naming drift for manually named worktrees | Active |
| 2026-03-04 | Standardize 3-stage promotion path (`feature branch -> Temp-Phase-and-View-2 -> main`) | Ensure primary worktree always stages feature deliveries through the creator branch before production merge | Improves promotion traceability and keeps primary integration flow consistent | Superseded |
| 2026-03-13 | Rename shorthand to `sync worktrees` | Match revised parallel worktree terminology | Clarifies shorthand request handling across repo docs and skill mirrors | Active |
| 2026-03-13 | Make creator-branch sync the default worktree handoff | Ensure the primary branch refreshed depends on the branch that created the feature worktree | Removes hardcoded staging-branch assumptions and separates optional `main` promotion | Active |
| 2026-08-02 | Rename ribbon panel `Print.panel` to `Sheet.panel` | Panel now hosts sheet management beyond printing (Sheet Manager) | Panel title follows folder name; only test path references needed updates | Active |
| 2026-08-02 | Extract shared revision helpers to `lib/easybim/sheet_revisions.py` | Add/Remove Revision fallback chains and cloud hide/unhide were duplicated at module level in Revision Manager scripts and un-importable | Sheet Manager reuses one tested implementation; Revision Manager scripts intentionally unchanged (optional migration later) | Active |
| 2026-08-02 | Sheet Manager stages all edits and commits only via Apply Changes | Single assimilated undo step, red-preview of every pending change, safe two-phase renumbering | All model writes flow through `sheet_manager_revit.apply_staged_changes`; cloud-driven revision unchecks route through consolidated hide/unhide confirmations | Active |
| 2026-08-02 | Sheet Manager Excel round-trip uses `easybim.excel_workbook` + hidden `_metadata` keys | Reuse the shared OOXML reader (no Excel install needed); `p:`/`tb:`/`rev:<id>` column keys keep round-trips unambiguous | Import matches by ElementId (number rescue only for stale ids); missing-id rows create sheets, duplicate numbers are red-flagged and skipped | Active |
| 2026-08-02 | Promote `View Template.pulldown` to a single `View Template.pushbutton` | Pulldown held one command; the reshaped tool covers view and view-template transfer in both directions | Ribbon icon-mirroring aliases retarget to the new title; old bundle paths removed | Active |
| 2026-08-02 | View Template transfer engine: keep the non-controlled sandwich for template targets, restrict-the-source (or a temp `CreateViewTemplate` template for view sources) for view targets | Views have no non-controlled parameter set, so the target-side sandwich cannot work on them; the sandwich is production-tested for templates | All writes flow through one assimilated `TransactionGroup`; temp templates never persist; per-target rollback isolation retained | Active |
| 2026-08-02 | Selective Transfer V/G rows are tri-state masters; fully-checked groups ride the native parameter mechanism, partial groups are written per item and forced-controlled on template targets | Per-item granularity was user-confirmed; group-level bits (e.g. non-overridden category visibility) only travel via the native mechanism; partial items on a template are inert unless the group is controlled | Nothing transfers unless explicitly checked (all selections default off) | Active |
| 2026-08-02 | Clash Detection Mode is forward-only: only elements changed after Start are ever tested | Gives the user "the clash you just made" without a whole-project pass; also why Start is instant and pre-existing clashes never appear | No baseline scan exists; a clash between a changed element and an untouched one still reports | Active |
| 2026-08-02 | Clash Detection subscribes `DocumentChanged` + `Idling` directly on Start and detaches on Stop, instead of adding pyRevit hook files | Hook files fire a script on every event for every session; direct delegates cost nothing while the mode is off and skip engine spin-up while it is on | `DocumentChanged` does set unions only; all geometry, popups and Show requests run from the budgeted Idling pass | Active |
| 2026-08-03 | All per-Idling work runs from one `Idling` delegate installed by `startup.py` (`lib/easybim/idling.py`); the `app-idling` hook file is deleted | Extends the Clash Detection decision below to the rest of the extension: pyRevit re-reads and recompiles a hook script on every Idling event (file read + compile + fresh scope + `ScriptRuntime` per tick, no compiled-code cache), which Revit raises continuously all session | Consumers get the delegate's own `sender` (`EXEC_PARAMS` returns stale data outside a script run); the delegate guards `SystemExit` as well as `Exception`, holds a re-entrancy flag for consumers that open modals, and detaches before the auto-update in case it reloads pyRevit; the envvar mirror is mandatory since pyRevit detaches its own hooks on reload but never a raw delegate | Active |
| 2026-08-02 | Clash Detection writes nothing to disk; all state is module globals mirrored to pyRevit envvars | User required no local file that grows, and the repo already deleted a debug log for the same reason | Stop has no cache to delete; `test_clash_detection_no_local_files.py` fails the build if a file API appears in any clash module | Active |
| 2026-08-02 | Clash Detection pushbutton sets `__persistentengine__ = True` (repo's first use) | The engine holds live Revit event delegates after the command returns; a recycled pyRevit engine would silently stop detection | Enforced by `test_clash_detection_command_names.py`; any future event-owning button needs the same flag | Active |
| 2026-08-02 | Dockable pane registered unconditionally from `startup.py`, with a modeless right-edge window as fallback | Revit only accepts `RegisterDockablePane` during app init, so it cannot be deferred to first use; pyRevit dockable-panel support could not be verified off-Revit | Registration is hidden and free; `clash_detection_panel` degrades instead of failing when the API is absent | Active |
| 2026-08-02 | Clash Detection queries return `(ids, completed)`; resolution runs only on completed queries and never on a pair recorded in the same pass | A bare `[]` meant "no clash", "no bounding box" and "the query threw" all at once, so one work item deleted the clash another had just found - which is why moved and copied elements went unreported | Fixes the create-vs-edit asymmetry; enforced by `test_clash_detection_state.SamePassProtectionTests` and a source check in `test_clash_detection_no_local_files` | Active |
| 2026-08-02 | Group/AssemblyInstance ids expand to their members instead of being dropped | Revit reports the container, whose category is on neither side, so a moved or pasted group vanished silently | Members flow through the normal queue; nested containers expand on their own turn | Active |
| 2026-08-02 | Pause does not queue edits; Resume re-validates the recorded pairs instead of replaying them | Paused should cost what off costs, and a long pause must not bank a backlog that floods Resume | One primitive (`_revalidate_pairs`) serves both Resume and Edit Categories, budgeted through the normal per-tick limits | Active |
| 2026-08-02 | Edit Categories rebuilds the side contexts in place rather than restarting the session | User asked to keep clashes that are still clashing; a restart would discard unreviewed work | `ElementInfo` carries `category_id` so scope is checked without re-reading elements | Active |
| 2026-08-02 | Ribbon state badge is drawn over the button's live image, not swapped for pre-baked icons | Keeps the dot correct in both Revit themes and survives pyRevit picking the icon variant at load time | No extra PNGs; `clash_detection_ribbon` restores the original image on stop | Active |
| 2026-08-02 | Clash setup UI moved from the pushbutton folder into `lib/easybim` | The dockable panel offers Edit Categories and cannot import upward out of `lib/` | `script.py` is a thin launcher; XAML lives with the other panels in `lib/easybim/ui/` | Active |
| 2026-08-03 | Clash Detection running flag mirrored into a pyRevit envvar | A recycled engine lost the module global while the .NET delegates stayed attached, so the mode kept detecting with every window reporting it off and no way left to stop it | `is_active()` survives reloads; `stop()` detaches through the envvar even when the session is gone; `has_live_session()` distinguishes the two | Active |
| 2026-08-03 | One main window: the ribbon button always opens the category window, which carries the live session controls | A closed panel was unrecoverable and Stop had no permanent home; a separate status window would have been unreachable once the ribbon opened the main window | `clash_detection_status.py` and its XAML removed as superseded; Open Panel / Pause / Resume / Stop live on the main window | Active |
| 2026-08-03 | Clash rows carry a checkbox per element, not per pair | A clash names two elements and the user often wants only one of them; Show now takes element keys | `request_show` takes element keys; `_show_elements` replaces the pair-based version | Active |
| 2026-08-03 | Clash Detection drops its own `easybim.clash_detection*` modules on launch when no session is running | `__persistentengine__` keeps `sys.modules` alive across a pyRevit reload (that is what keeps the event delegates alive), so the documented "pull and reload" update flow kept running old code until Revit restarted | Updates land on the next click; the live pane and its rows are mirrored to envvars so a re-imported module still drives the pane Revit already created | Active |
| 2026-08-03 | Tag Align stores every reference offset twice - along the view axes and along the element's own in-plane frame - plus the source view scale, instead of one baked-in vector | One reference has to serve both "always 2 ft above on the sheet" and "always 2 ft perpendicular to the run", and the view-scale and 180°-flip options must be togglable after the pick without re-reading the model | `compose_target_offset` picks the frame per target; `comparable_offset` compares in printed or model units depending on the option, so the same toggle also decides whether two references are duplicates or a conflict | Active |
| 2026-08-03 | Tag Align enforces one tag family type and one element category per reference set, with exactly two scopes (exact type, or paired family as the default) | User decision: a cross-category or cross-tag-type reference has no unambiguous offset, and the "all families" scope that was originally sketched had no resolution rule | `TAG_TYPE_MIXED` and `CATEGORY_MIXED` are fatal and discard the set; `POSITION_CONFLICT` and `SCOPE_AMBIGUITY` are resolvable per group, and validation reruns on every scope/option change because the scope itself can create ambiguity | Superseded |
| 2026-08-03 | Tag Align scope is a three-rung fallback ladder - all families in the category (default), paired family, exact type - rather than a filter | User asked for the category-wide option as the first and default choice; the resolution rule the earlier decision was missing is that `candidates_for_target` always prefers the narrowest match available and the scope only sets how far it may fall back | Widening the scope only ever adds a fallback, never steals a target from a closer reference; `scope_key` mirrors the ladder so category scope groups every reference together and two families at one orientation correctly collide; `narrower_scope` powers the conflict window's one-step-down button | Active |
| 2026-08-03 | Tag Align derives its element direction from a location curve before a family instance's hand orientation | The curve tangent is what the eye reads as the direction of a wall, pipe or beam, and it is an undirected axis - which is what lets a wall drawn right-to-left match one drawn left-to-right under the default `collapse_flip` | Folding to 180° only ever applies when both sides are `axis_symmetric`; a door rotated 180° genuinely faces the other way and keeps its own bucket | Active |
| 2026-08-03 | Tag Align splits every write into `build_plan` (complete dry run) then `execute_plan`, with batch writes on a raw `DB.Transaction` | The confirmation dialog must report exactly what the write will do, and a cancelled progress bar has to roll a whole batch back without raising - `revit.Transaction` cannot roll back without an exception | Preview and write cannot drift; counters are zeroed on rollback so the report never claims work that is gone; click-at-a-time keeps one transaction per click so undo walks back click by click | Active |
| 2026-08-03 | Tag Align matches on names (category signature + family + type) instead of ElementIds | An ElementId means nothing in the next document, so a saved preset could never be loaded into another model; within one document `family|type` is exactly as discriminating, because Revit cannot hold two types of the same name in one family | Presets are portable by construction and the mixed-tag-type / mixed-category checks still work on rules whose ids are not bound yet; renaming a family or type breaks that reference in an old preset, which the load reports rather than silently rematching | Active |
| 2026-08-03 | Tag Align presets have three destinations: roaming AppData, Extensible Storage inside the model, and a user-set shared folder | They answer three different questions - survives a reopen, follows you to another model, reaches your team. No Revit API writes files into ACC Docs, so the only things that reach a cloud-model team are data inside the model (syncs to central with it) and a file on an ACC Desktop Connector path | `get_universal_data_file` is used rather than `get_data_file`, whose Revit-version stamp would silo presets per release; the shared path lives in the local payload so the feature has one storage mechanism; the in-model source takes injected callables so `tag_align_presets` stays importable and testable without Revit | Active |
| 2026-08-03 | Update Circuit Rating takes the source value from the circuit's *elements*, not the circuit, and picks the highest | User decision: the rating is nameplate data on the equipment, and where several pieces of equipment share a circuit the highest-rated one - usually a downstream panelboard - is what defines the circuit. Partial coverage still counts, so one element carrying the parameter is enough to update the circuit | A tie is broken in favour of an Electrical Equipment element and then by label, so the pick never depends on element order; the donor is shown in the grid because "highest wins" is only auditable if the winner is named | Active |
| 2026-08-03 | A blank or zero reading is not a rating: it contributes no sample, and a circuit with no sample is dropped from the list rather than shown | An unset Double parameter reads as `0.0`, not as empty, so treating it as a value would let one all-empty parameter zero every circuit it touches. Circuits that cannot be updated must not be listed as if they could | The skipped count under the grid is the only place they appear; it also keeps the source picker down to parameters somebody has actually filled in | Active |
| 2026-08-03 | The zero-amp report is read back from the committed model and covers every circuit in the project, not the plan's circuits | The plan says what was intended, not what the model holds - a write can be refused, and the circuits most worth reporting are the ones the run could never touch (no value on any element, or unticked). Reading after the transaction commits makes the report answer "what is left to do" instead of "what did I just do" | `read_circuit_rating_values` re-collects every circuit; a circuit is reported when *any* ticked target reads zero, blank, or missing, so unticking a target also removes it from the test | Active |
| 2026-08-03 | Tag Align rebinding only requires the host category and the tag type to resolve | A missing host family or type is not an error: under the category scope a reference from another project still governs every other family in that category. A missing tag *type* only blocks creation, not alignment, because tags are matched to a reference by tag family name | The load reports unresolved rules and substitutions instead of failing; `Align` stays available with a missing tag family while `Align & Tag` greys out with the reason and `change_tag_type` is forced off | Active |
| 2026-08-03 | Load Parameters handles shared parameters only, and materialises every one of them through a single temporary shared parameter file whose header is written by hand but whose `*GROUP`/`*PARAM` lines are written by Revit | `AddParameter` and `BindingMap.Insert` both need an `ExternalDefinition`, which only exists inside a `DefinitionFile` - a shared parameter read out of a project has none. Letting `Definitions.Create` write the body avoids hand-mapping `ForgeTypeId` onto the legacy `DATATYPE` tokens (no public helper, version-sensitive), keeps our own bytes pure ASCII so encoding stays Revit's problem, and gives project-sourced and file-sourced parameters one code path. Only a shared parameter has a GUID, and the GUID is what makes the family's parameter the same one the project knows | `options.GUID` preserves identity; the file is held current for the *whole* run because an `ExternalDefinition` is a live handle onto an open `DefinitionFile`; `SharedParametersFilename` is restored in a `finally` **before** the delete, since it persists into Revit's `.ini` across sessions; `Family Type` specs are refused because their `DATACATEGORY` cannot be expressed, and same-name/different-GUID pairs are caught at plan time because `Definitions.Create` throws on a duplicate name | Active |
| 2026-08-03 | Load Parameters writes its own `IFamilyLoadOptions` with `overwriteParameterValues = False`, rather than reusing Families Transfer's | The two tools want opposite answers: `True` is right for a transfer and wrong for "add a parameter and put the family back", where the project's existing type values must survive. Sharing the class would guarantee one of them silently drifts wrong | Enforced by `test_load_parameters_command_names.LoadParametersContractTests`, which fails the build if `True` reappears in the module | Active |
| 2026-08-03 | Folder `.rfa` families are saved to disk only, never loaded into the project, and run *after* the project pass outside the `TransactionGroup` | A file write has no transaction, so a cancel cannot un-write it - putting it inside the group would imply an undo that does not exist. Running it last means cancelling in the common case costs nothing. `OpenDocumentFile` + `Save()` also silently upgrades an older family to the running Revit version, which would convert a whole library with no way back | `BasicFileInfo.Extract` reads the authoring version from the header without opening the document, so the confirmation can name the files and require an acknowledgement tick; an optional output folder mirrors the tree instead of overwriting; the summary keeps two buckets so a cancelled run reports rolled-back families and already-written files separately | Active |
| 2026-08-03 | An existing project binding is merged, never replaced | `ParameterBindings.ReInsert` overwrites the whole `CategorySet`, so rebinding a parameter for one new category would silently unbind every category the user did not happen to tick that run | The current categories are read and unioned before `ReInsert`; an Instance/Type flip is reported as its own line in the confirmation and needs its own acknowledgement, because Revit discards the parameter's values when the binding kind changes | Active |
| 2026-08-04 | Linked Sheets Copy measures the placed viewport *last*, after a `doc.Regenerate()`, instead of solving the anchor in a precheck pass | `View.Outline` is derived: the crop, the annotation crop, the scale and a view template all move it, and it stays stale until the document regenerates. `View Align` solves its anchor before writing the crop, which is why its Assign Scope Box and Assign Crop Region options can misplace a viewport | There is exactly one measurement per viewport and it follows both configuration and regeneration, so an imperfect crop reproduction moves the viewport *border* and never the drawing; pinned by `test_linked_sheets_copy_command_names` asserting the call order inside `place_and_align` | Active |
| 2026-08-04 | The anchor solver moved to `lib/easybim/sheet_geometry.py` and `View Align` was switched over to it | Two copies would let "align" and "copy and align" drift apart - the exact failure the Conventions section warns about - and `View Align` had no tests at all | `test_sheet_geometry.py` is the first coverage that maths has ever had; the module is written against duck-typed vectors so it runs off Revit. One behaviour change: a view with `Scale == 0` is now refused with a reason instead of being silently treated as 1:1 | Active |
| 2026-08-04 | Linked Sheets Copy is Revit 2024+ with no fallback path | `RevitLinkGraphicsSettings` and `View.SetLinkOverrides` do not exist before 2024, and "show this link as it appears in that linked view" *is* the tool. A 2023 mode would create sheets and views that show the wrong thing | `min_revit_version: 2024` plus a runtime `hasattr` re-check; no degraded branch exists to rot | Active |
| 2026-08-04 | Levels are matched from Copy/Monitor first, then name, then elevation, with a dropdown that always wins | Copy/Monitor is the only rung that records a decision somebody already made; the other two are guesses and the window says which rung was used. Filtered to the *selected link instance*, because a host level monitoring the same level in another instance says nothing about this one | An unmapped level skips its views and still builds the rest of the sheet, so one missing level never costs a whole sheet; two host levels monitoring one linked level resolve to the first and are flagged rather than blocking | Active |
| 2026-08-04 | View-level annotations are not copied by default; sheet-level content is the only thing the title-block options turn on | With the link shown By Linked View, the linked view's own text, detail lines and filled regions already draw through the link, so copying them draws everything twice. A sheet is not part of the linked model's geometry, so nothing on it can arrive that way and copying is the only route | The option is off, warned in its tooltip and in the confirmation, and gated behind an acknowledgement tick; tags, dimensions and view references are never copied at all because their references do not survive the document boundary | Active |
| 2026-08-04 | Every cross-document copy goes through one `CopyPasteOptions` factory whose `IDuplicateTypeNamesHandler` answers `UseDestinationTypes` | Without a handler Revit either raises or puts a modal dialog in the middle of a batch; `UseDestinationTypes` is the only safe answer, because a type here that shares a name with one in the link is *this* model's type and a copy must never redefine it. **(Corrected 2026-08-14: the alternative named in this row, `UseOtherTypes`, does not exist - the enum is `UseDestinationTypes` and `Abort`.)** This is the repo's first `IDuplicateTypeNamesHandler` | `test_linked_sheets_copy_command_names` fails the build if `DB.CopyPasteOptions()` appears at more than one call site, so a new copy cannot forget the handler | Active |
| 2026-08-04 | The host title block is moved to the linked instance's position on the sheet | Sheet coordinates are absolute and the viewports are aligned to the *linked sheet's* absolute coordinates; a title block at a different spot puts every correctly-placed viewport in the wrong place on the page | `ViewSheet.Create` drops the title block at the family's own origin, so the move is unconditional wherever a title block was placed | Active |
| 2026-08-05 | A Revit link's per-category visibility cannot be read or written by any API through 2026, so no tool can sync a "Custom" link display back to its linked view | Verified against `RevitAPI.xml` 2023-2026: `RevitLinkGraphicsSettings` has 23 members and none touches categories; `RevitLinkInstance` has 5 and none touches display; no method anywhere takes a link id and a category id together; `View.HideElements` explicitly excludes links; `Category.Visible(View)` and `View.GetCategoryHidden` are host-only. Host view filters do reach a link via `ViewFilterType = ByHostView` but also hit host elements, so they cannot express "hide Ceilings in the arch link but keep mine" | Any future link tool must treat the frozen category table as opaque: transport it whole (`GetLinkOverrides` -> `SetLinkOverrides` between views, which `view_template_transfer_revit._apply_link_item` already relies on), or work around it. What *is* readable: the nine 2025 Basics properties, and the whole linked document through `GetLinkDocument()` | Active |
| 2026-08-05 | "Frozen" is diagnosed by inference and must always be labelled as such | The category tabs are unreadable, so the only available evidence is that a link is `Custom` while all nine readable Basics properties still say `ByLinkView` - nothing on the Basics tab put it into Custom, and the only other door is a category or workset tab. It is sound but it cannot say *which* tab, and a view whose tab was set to Custom and then manually back to "by linked view" reads as a false positive | Any future implementation must label it "(inferred)" in the value itself rather than in a comment, so no caller can present it as measured, and must pin the nine Basics members literally in a test - a tenth property in a future Revit would otherwise quietly weaken the diagnosis. Two further traps found on a real model: only `ByLinkView` requires a valid `LinkedViewId`, so `Custom` with none is legal and must not be called broken; and a link following no linked view cannot be stale at all, whatever its category tabs hold | Active |
| 2026-08-05 | Link geometry cannot be used to observe which categories a link draws - the last workaround, tested on a real model and closed | A `RevitLinkInstance` does not materialise the linked model's geometry in the host document. `get_Geometry` returns `None` with no view and an empty `GeometryElement` with one, identically for a plan view and a 3D view, with and without `IncludeNonVisibleObjects` and at every `DetailLevel`; the instance reports no model extents from `get_BoundingBox(None)`. So there is nothing to walk and no way to infer per-category visibility by observation | Per-category state inside a link is unreadable by *every* route now tested: no API surface, no host view filter that does not also hit host elements, no geometry. A tool can read which linked view a link points at and all nine Basics rows with their values, and can read the linked document itself in full - it can never read or write the five category and workset tabs | Active |
| 2026-08-05 | Revit 2024.2 added nothing to the link display API; the full Custom surface is 2025 | Diffing the 2024.0 and 2024.2 doc XMLs shows 32 new members, all STEP import / FDX / Datasmith / point-elevation and none link-related. 2024 exposes only `LinkVisibilityType` and `LinkedViewId`. The widely repeated claim that 2024.2 added Custom support is false | Anything needing the nine Basics properties is `min_revit_version: 2025`, re-checked at runtime with `hasattr(RevitLinkGraphicsSettings, "ObjectStyles")` rather than a version number - a property that is not there cannot be read, which is all the caller cares about | Active |
| 2026-08-14 | The load options are fetched once per transfer and held for the batch, on evidence from the IL rather than the docs | Disassembling `RevitAPI.dll` (identical 2021-2026) settles what the XML never mentions. `GetRevitUIFamilyLoadOptions` returns a **fresh** managed wrapper every call - `ProxyToPublic` ends in an unconditional `newobj IFamilyLoadOptionsProxy` - but that wrapper is **stateless**: one private field `m_proxy`, one internal `.ctor(object)`, no booleans. The state that would remember "Do this for all loading families" is on the far side of `FamilyDBAPI`, which Autodesk does not ship in the NuGet package, so it cannot be settled statically. What *can* be: `PublicToProxy` unwraps our held wrapper back to the **same native `IFamilyLoadOptionsBase*`** on every `LoadFamily`, so one instance gives Revit one identical native handler across the run | Two consequences. The checkbox spanning the batch is *possible* only with this arrangement - untested, needs Revit. And `IFamilyLoadOptionsProxy` is `IDisposable` with a **finalizer** that disposes the native object, so fetching per family would leave each previous wrapper collectable mid-loop and tear down a live handler. Also confirmed: **no batch-load API exists** - all five `LoadFamily` and three `LoadFamilySymbol` overloads take a single path or document, and no member anywhere accepts a collection of families | Active |
| 2026-08-14 | The overwrite prompt is **ours**, built from `TaskDialog` command links plus `VerificationText`, replacing `GetRevitUIFamilyLoadOptions` | Revit's own dialog carries the same "Do this for all loading families" checkbox, but this tool calls `LoadFamily` once per family and Revit scopes that tick to a single call - so it asked again for every clash, which is the complaint. Ours remembers the answer on the load-options object, which spans the batch | Same widget, so it looks like the native prompt; `TitleAutoPrefix = False` or Revit prepends the command name. `VerificationText` and `WasVerificationChecked()` are `<since>` 2011 and present in every version from 2021, so no floor is needed - the no-prompt path exists only for an exotic host and falls back to the original silent overwrite. Traps confirmed against the metadata: `WasVerificationChecked` is a **method**, not a property; setting `ExtraCheckBoxText` alongside `VerificationText` makes `Show()` throw; `TaskDialogResult` and `TaskDialogCommonButtons` give different values to the same names and must never be cast | Active |
| 2026-08-14 | The prompt is raised from **inside** `IFamilyLoadOptions.OnFamilyFound`, which no other interface implementation in this repo does | Every other handler here answers silently, and `lib/easybim/copy_paste.py` exists specifically to stop an *unanswered* callback raising Revit's modal mid-batch. This is the opposite case - a modal raised on purpose at the one point Revit's own implementation raises the same widget. It is also the only way to ask **exactly when Revit would**: the callback fires only when the family is present *and* changed, so a byte-identical family loads with no prompt, which asking before the load could not achieve | Nothing in the API docs forbids it, and `GetRevitUIFamilyLoadOptions` is documented to do precisely this. The one documented rule that does apply: `TaskDialog.Show` throws off Revit's main thread, so family loading must never move to a worker thread. `OnFamilyFound` carries no family name, so `begin()` leaves one on the options object before each load | Active |
| 2026-08-14 | A declined overwrite is read from our own flag instead of the exception message | Returning `False` from `OnFamilyFound` makes `LoadFamily` **throw** rather than return false. The previous code inferred that from the exception type plus a `"cancel"` substring, because Revit's dialog answered where we could not see. Now we return the `False`, so the options object simply records it | `_is_declined_overwrite` is gone along with the guarded `InvalidOperationException` import; classification is now fact, not pattern-matching | Active |
| 2026-08-14 | **Correction:** Revit's "Family Already Exists" dialog **does** carry a "Do this for all loading families" checkbox; an earlier row and `README.md` said it did not | The claim came from grepping `RevitAPI.xml` and `RevitAPIUI.xml` for "apply to all", "for all remaining" and "do this for all" and finding nothing. Those documents describe the **API**, not Revit's own dialogs, whose strings live in native resources - so silence there was never evidence about the UI. Same habit produced the `UseOtherTypes` error the same week: **an absence in the docs is only evidence about what the docs cover** | What remains true and load-bearing: the object is opaque, so the answer cannot be read back, which is why "loaded" vs "overwritten" is decided by asking the target. What is now open: whether the checkbox spans separate `LoadFamily` calls. `transfer_families` creates **one** options object for the whole batch (outside the per-family loop), which is the only arrangement that could let it - now pinned by a call-count test, because the previous assertion could not catch a per-family fetch | Active |
| 2026-08-14 | Families Transfer hands Revit its own "Family Already Exists" prompt via `UIDocument.GetRevitUIFamilyLoadOptions()`, keeping the silent-overwrite options only as a UI-less fallback | Overwriting was already happening on every clash - the tool answered `True` with `overwriteParameterValues = True` and never showed the question. Replacing somebody's family without telling them is the wrong default when Revit has a perfectly good dialog for exactly this | `RevitUIFamilyLoadOptions`, which the `LoadFamily` docs name, is **not an instantiable type** - absent from assembly metadata in 2021-2026, and the only implementer of `IFamilyLoadOptions` there is a non-public proxy. The static accessor is the only door. The returned object is opaque, so the user's answer cannot be read back - which is why "loaded" vs "overwritten" comes from asking the target. **Corrected 2026-08-14: an earlier version of this row claimed there is no apply-to-all. Revit's dialog does carry a "Do this for all loading families" checkbox** - see the correction row below. A declined load surfaces as `InvalidOperationException`, not a `False` return, and is filed under `skipped` - never `failed`, and never touching `summary.cancelled`, which the progress bar owns | Active |
| 2026-08-14 | "loaded" vs "overwritten" is decided by asking the target, not by a flag our load options set | Revit's own options object answers the prompt itself and cannot report back, so `loaded_existing` stopped being knowable. The same read also tells a declined overwrite apart from a real failure - the prompt only appears when the family was already there | Reuses `_target_family_names`, the per-target name cache rung 3 already builds; no second mechanism. `FamilyTransferLoadOptions` lost `loaded_existing` and `reset()` with it | Active |
| 2026-08-14 | **Correction:** `DuplicateTypeAction.UseOtherTypes` does not exist, and this repo documented it as if it did | The 2026-08-04 row and `lib/easybim/copy_paste.py` both described `UseOtherTypes` as redefining the destination's type. A raw identifier scan of `RevitAPI.dll` returns zero hits in 2021.0.0, 2021.1.9, 2024.0.0, 2024.2.0, 2025.0.0 and 2026.0.0; the enum is `UseDestinationTypes = 1` and `Abort = 2`, nothing else. Two tests asserted `assertNotIn("UseOtherTypes")` and passed against a phantom | `UseDestinationTypes` is still correct, but because the only alternative is **cancelling the paste**, not because it beats a redefine option. There is no overwrite anywhere in the copy/paste API - Autodesk shipped `GroupLoadOptions.ReplaceDuplicatedGroups` for groups in 2024 and deliberately no analogue. Both tests now assert the handler never returns `Abort`, which has teeth | Active |
| 2026-08-14 | Export warns once, before it starts, if the folder already holds files it would write | It wrote with `OverwriteExistingFile = True` and replaced silently. Asked once for the batch rather than per file, and `planned_export_filenames` runs the same sanitising and `_2` de-duplication the export itself uses, so the warning cannot name a different set than it replaces | Offers Replace / Choose a different folder / Cancel. No per-file prompts | Active |
| 2026-08-12 | `BasicFileInfo.Format` is the version to read; `SavedInVersion` is a pre-2020 fallback only | `BasicFileInfo.SavedInVersion` was **removed in Revit 2020** - it is in the 2015-2019 DLLs as `get_SavedInVersion` and absent from 2020 through 2026. Load Parameters read it through `getattr(info, "SavedInVersion", u"")`, so on every Revit that tool supports (`min_revit_version: 2023`) the default swallowed it, `saved_in_version` was always empty, and the older-family warning plus its acknowledgement tick - the safety check the 2026-08-03 folder-families decision is built on - silently never fired | `Format` is documented as the major release ("2019") and is read first, with the old name kept only as a fallback for a 2015-2019 host. A truthiness guard was added alongside: `_version_number("")` returns 0, and 0 is less than any host version, so an unreadable header used to satisfy the comparison. `test_load_parameters_command_names` now asserts `Format` is read before `SavedInVersion` and that the guard is present | Active |
| 2026-08-11 | The three source cards are one shape, and every star row in `SourceSelectionWindow.xaml` carries a `MinHeight` | The cards were built at different times: two sat in unconstrained star rows and one was `Auto`, so dragging the window squashed the first two - headers and buttons included - while the third stayed put. A WPF star row takes its minimum from `RowDefinition.MinHeight` only; unlike `Auto` it does not fold its child's desired height in, so it collapses to zero and paints the child outside the cell. `MinHeight` on the child `Border` cannot fix that, because the Grid still arranges into the short cell | Header/count/buttons are `Auto` rows and the list is the only star row inside each card, so dragging resizes the three lists and moves nothing else. Header lines use `TextTrimming`, never `TextWrapping` - a wrapping header is a variable-height header, which is the thing being fixed - and the same goes for the footer status line. `test_no_source_card_can_be_squashed_to_nothing` fails the build if a star row loses its `MinHeight` | Active |
| 2026-08-11 | Revit links moved off page 1 into their own page; card 3 now lists the families chosen *from* links | A card that listed links while its two neighbours listed families made the page read as three unrelated things, and it was the card that could not resize. Links are a step, not a source: the source is the families they yield | Three pages - card 3, then `LinkSelectionWindow`, then the family browser. `LinkSelectionWindow` is a new class rather than a reuse of `TargetSelectionWindow`, which has no search, no count, no `_is_ready` guard and no disabled-row handling, and whose title is pinned by a test with `assertNotIn`. Both Next and Back prune the chosen families against the ticked links, because page 1 no longer has the link checkboxes that used to do that | Active |
| 2026-08-11 | `Hide Un-checked` is a rebuild-time filter on the second pages, not a live per-row one | A row that vanishes the instant it is unticked cannot be un-vanished. Applying the filter only on rebuild means the visible set may legitimately hold unticked rows until the next explicit refresh, which is what makes the checkbox usable | `select_all_click` still skips the rebuild even with the filter on - it only touches rows already on screen and cannot reveal a hidden one - while `select_none_click` must rebuild, and must sync the expanders first or every category springs open. The invariant that licenses leaving hidden rows out of the control sync: a row hidden by this filter is always an unticked row, so it never holds state a sync would need. Unloaded links can be hidden by it, which contradicts "a missing link looks missing" - allowed, because it is an explicit user action | Active |
| 2026-08-11 | Tags reach their family through `GetTypeId()`, guarded by an `isinstance(symbol, DB.FamilySymbol)` test | Exactly two classes in the whole Revit API declare `Symbol`: `FamilyInstance` (so model families and `AnnotationSymbol` generic annotations already worked) and `TextElement`, whose `Symbol` returns a `TextElementType`. `IndependentTag` derives from `Element` and has no `Symbol` at all; its type is a plain `FamilySymbol`, and there is no `IndependentTagType` class. Room/area/space tags are the same shape via `SpatialElementTag` | The isinstance guard is load-bearing, not defensive - without it a text note carries a non-`FamilySymbol` into the next step. Text notes, dimensions, spot dimensions and detail lines stay correctly unpickable. `AllowElement` must stay fully try/except-wrapped: an exception there is documented to reject the element silently | Active |
| 2026-08-11 | Matchlines are not families and never can be, so nothing in this repo can transfer them | Verified against `RevitAPI.xml`/`.dll` 2021-2026: across 20,121 types there is no `Matchline` class and no `MatchlineType`; the only hits are `BuiltInFailures.MatchlineFailures` and four sketch-extent instance parameters. There is a `BuiltInCategory.OST_Matchline`, but a category is not a family, and `GetTypeId()` on one returns `InvalidElementId` | A matchline can never appear in `OfClass(DB.Family)`; its graphics come from Object Styles and move via Transfer Project Standards. The contrast is worth keeping: section heads, callout heads, elevation marks, grid heads, level heads and view reference *tags* **are** loadable families and are already listed - they just cannot be picked by clicking, because the clickable element is the system element that references them | Active |
| 2026-08-10 | Families from a Revit link are extracted by a three-rung cascade - the open copy of the same file, then `link_doc.EditFamily`, then `CopyElements` into the target - probed once per link before the browser opens | `GetLinkDocument()` hands back a fully readable document (recorded 2026-08-05), but *reading* a linked document and getting a family *out* of one are different questions: `Document.EditFamily` is documented to throw on a read-only document, and `GetLinkDocument` is documented to forbid operations that modify the document's status in memory - which `EditFamily` does, since it raises when a family is already being edited. Which way Revit actually goes cannot be settled off Revit, so the tool is built so that either answer works | The probe costs one `EditFamily` per link, not per family, so a refusal is one sentence rather than one failure line per family; `TransferSummary.notes` carries it above the detail list. The copy rung cannot overwrite - `UseDestinationTypes` resolves a name clash in favour of the target - so a family already in the target is skipped and says so rather than reporting a success that changed nothing. Export has no third rung, because writing an `.rfa` needs the family document the link refused. `test_families_transfer_links.py` drives all three rungs against fakes | Active |
| 2026-08-10 | A link family key is `link\|<link document key>\|<element id>`, and `Family.IsEditable` is recorded on link rows rather than used as a gate | An ElementId means nothing outside its own document, so two links can both hold family 12345 and would otherwise collapse onto one row. `IsEditable` is documented in terms of in-place, saveable and primary-family - links are not mentioned - so its value inside a link is unverified; gating on it would empty the card, which reads as "this link has no families" rather than as a refusal, the single worst outcome | `is_transferable_family` takes `require_editable`, keeps the in-place rejection unconditional, and never touches `FamilyCategory`/`CategoryType` (pinned since the tool was built). Unchecking a link drops the families chosen from it, so a link that is off cannot still transfer | Active |
| 2026-08-10 | The `CopyPasteOptions` factory and its `UseDestinationTypes` handler moved from `Linked Sheets Copy` into `lib/easybim/copy_paste.py` | The 2026-08-04 decision was written as repo-wide but enforced by a test that read one file, so `Batch Duplicate Host` and `Sheet Manager` already had their own call sites. A second tool needing it made the shared home worth building rather than copying twenty lines a third time | `test_families_transfer_command_names` and `test_linked_sheets_copy_command_names` both now assert one `DB.CopyPasteOptions()` repo-side and the shared import on the consumer. `Batch Duplicate Host` and `Sheet Manager` are untouched and still carry their own; migrating them is a separate, optional change | Active |
| 2026-08-03 | Load Parameters' window stays open after a load; both actions live on it | Picking parameters and setting their group/instance is the expensive part, and it is identical for both targets - closing would make "families, then project" mean doing it twice. The close-and-reopen loop the other tools use exists only because they need Revit's pick UI, which a modal dialog blocks; nothing here does | Each action runs its dry run, confirmation, progress bar and report as nested `ShowDialog` calls and returns to the still-populated window; the target lists are re-rendered afterwards because a run can mark rows unusable (checkout, read-only files) | Active |
| 2026-08-14 | Sheet Manager opens modeless; every Revit-touching action from the open window runs through `lib/easybim/external_events.py` (repo's first `ExternalEvent`: one `IExternalEventHandler` draining a FIFO of callables inside `Execute`, injectable event factory) | The user must keep operating the model with the grid alongside, and Select Title Blocks must highlight immediately - a modal window freezes Revit and never repaints a selection | Bridged `work(uiapp)` + `on_done` both run inside `Execute` (Revit main thread = WPF thread), so dialogs and grid updates need no marshalling; the window binds its document at launch, refuses writes when another document is active, and disables Revit buttons/grid edits while a request is pending; modal fallback when the event cannot be created | Active |
| 2026-08-14 | Sheet Manager sets `__persistentengine__ = True` (repo's second use) and drops its own modules from `sys.modules` on launch while no window is open, gated by `EASYBIM_SHEET_MANAGER_ACTIVE`; window mirrored to `EASYBIM_SHEET_MANAGER_WINDOW` for single-instance re-activation | The ExternalEvent handler and the window are Python objects that must outlive the command run; a persistent engine keeps stale modules alive across a pyRevit reload (2026-08-03 Clash Detection decision) | Pull-and-reload updates land on the next click; a second ribbon click activates the open window instead of opening another | Active |
| 2026-08-14 | Sheet Manager freshness is tiered: light sync on window Activated (numbers/names/additional revisions/tblock counts/new+deleted sheets, throttled 2 s, skipped while busy), full reload behind a Refresh button, and a pre-Apply recheck of every staged cell inside the same `Execute` as the write; a stale cell is skipped + reported and stays red against the updated baseline. `DocumentChanged` subscription rejected for v1 | The user preferred auto-refresh if it does not slow the tool: the light sync costs ~20-60 ms at 500 sheets and only runs on focus return; the heavy reads (cloud inventory, LookupParameter per cell) stay behind a click; skipping a stale cell never overwrites a value the user did not see | `state.merge_row_values` / `partition_stale_changes` / `mark_row_missing` are pure and unit-tested; revisit the `DocumentChanged` option only if a real model shows the light sync above ~150 ms | Active |
| 2026-08-15 | A Revit family cannot be downgraded by any API; Families Downgrade is a **rebuild**, never a file conversion, and says so on its first page | `SaveAsOptions` carries exactly `Compact / MaximumBackups / OverwriteExistingFile / PreviewViewId / (Get\|Set)WorksharingOptions` in every XML from 2021 to 2026 - no target version anywhere - and the `.rfa` container is a proprietary, version-specific format nothing inside a newer Revit can write for an older one. Every building block of a rebuild does exist on both ends: `Document.Export(..., SATExportOptions)`, `Document.Import(String, SATImportOptions, View)`, `FreeFormElement.Create` (2014+, family docs only), `ConnectorElement.Create*Connector(doc, ..., Reference planarFace)`, `FamilyManager.AddParameter/NewType/Set/SetFormula`, `Family.FamilyCategory` (settable), `NewFamilyDocument` + `FamilyTemplatePath`, `NewSymbolicCurve/NewModelCurve/NewReferencePlane`, `Categories.NewSubcategory`, `GenericForm.Get/SetVisibility` | One button, two modes: the newer Revit writes a package per family (SAT solids + a JSON manifest of everything else), the **same button** in the older Revit rebuilds native `.rfa` files of that release. Rebuilt solids are static; nested families are flattened; voids, dimensions, labels, type catalogs, hosted openings and 2D families are not carried - each is a per-family note in the report, never silent. Running both halves in one release is the built-in fidelity test | Active |
| 2026-08-15 | The ForgeTypeId **string** is the cross-version key for parameter data types and groups; legacy enums are reached by name normalisation plus a quirk table, and every version-dependent call is chosen at runtime by asking the API what it has | Verified across the six XMLs: `FamilyManager.AddParameter` takes `(str, BuiltInParameterGroup, ParameterType, bool)` on 2021, both that and `(str, ForgeTypeId, ForgeTypeId, bool)` on 2022, only the ForgeTypeId overload from 2023 (`ParameterType` gone 2023, `BuiltInParameterGroup` gone 2025, `GroupTypeId` class 2024+, `ParameterUtils.GetBuiltInParameterGroup` 2022-2024 only). 2021 already has `SpecTypeId` for every measurable spec and `UnitUtils.GetUnitType(ForgeTypeId)`, and `UnitType` names normalise onto `ParameterType` names for all but ten irrelevant members; `GroupTypeId` property names normalise onto `PG_*` for 108 of 125, with seven spelling quirks (`CurtainGridn1` -> `PG_CURTAIN_GRID_1`, `Termination` -> `PG_TERMINTATION`, ...) and ten groups newer than 2021 (-> Other, noted) | `HostCapabilities` reads `Definition.GetDataType` (2022+ => the ForgeTypeId overload) rather than a version number; the mapping tables live in the pure state module and `test_families_downgrade_state.EnumCoverageTests` re-checks the coverage claims against the NuGet XML/DLL whenever they are on the machine, skipping elsewhere | Active |
| 2026-08-15 | Geometry travels as one SAT (or DWG/ACIS) file per **attribute group**, exported through a scratch 3D view per group with everything else hidden and retried at the other detail levels when the export carries no body | An SAT body carries no material, subcategory or visibility, so solids that the rebuild must treat differently cannot share a file; solids treated the same can, which keeps the export count small (usually 1-5). Revit's `Export` writes what the view shows, so a form shown only in Coarse writes nothing at Fine - hence the retry ladder. The single-view export may suffix the view name, so the file is found by folder diff and renamed. Revit 2022 added ten model categories 2021 lacks (verified against the 2021 assembly); each maps to the category people used before, named in the report. Revit's SAT writer version is not selectable, so a **DWG with ACIS solids** (`ExportOfSolids = ACIS`, `FileVersion = R2007`) option hedges an older reader refusing a newer SAT | Import is `Origin`/`Default` unit then self-checked against the exported bbox (unit candidates Foot first, `MoveElement` for an offset, both reported); bodies become `FreeFormElement`s or, if one refuses even after `SplitVolumes`, the group keeps its import instance and says so; connectors go on the face carrying their exported origin (the API places at the face centre, so the miss is reported; `CONNECTOR_ANGLE` restores rectangular orientation). The source is never saved: `EditFamily` documents are closed without saving, an opened `.rfa` rides a rolled-back `TransactionGroup`. `context: zero-doc` is the repo's first use, so Revit 2021 can rebuild with no project open | Active |
| 2026-08-15 | The family-selection machinery of Families Transfer moved verbatim into `lib/easybim/family_selection_{state,ui,revit}.py` with its XAML under `lib/easybim/ui/`, and Families Downgrade is its second consumer | The three source cards, the link pages, the browser, the collectors, the link cascade, the model pick and the folder picker are ~2,000 lines that both tools need identically; a copy would drift, which is the failure the Conventions section warns about. Families Transfer keeps its own step machine (`_run` untouched apart from the four XAML arguments) and its transfer/export/close code; the shared pages take a `title` so a consumer never shows another tool's name | The three Families Transfer test files follow the code: the state test reads the flat namespace the command sees, the command-names test walks bundle and lib windows through `(xaml, module, class)` triples, the links test pre-registers the lib modules under their dotted names before `exec_module`. `pick_more_family_options(uidoc, prompt)`, `prepare_link_documents(..., refusal_hint)` and `pick_folder(description, allow_new_folder)` are the parametrised seams | Active |
| 2026-08-15 | My Ribbon places **the live ribbon item itself** on the user's panel (the `modify_ribbon` mechanism), and its sources are **ordinary installed pyRevit extensions** - a GitHub link is cloned into `%APPDATA%\pyRevit\Extensions\<Name>.extension` with pyRevit's bundled LibGit2Sharp, a catalogue entry goes through `extpackages.install`, an installed extension is used as-is | The user's requirement, asked while planning: pyRevit's own Update and Reload buttons must keep the linked tools updated. `versionmgr.updater.get_thirdparty_ext_repos()` pulls every enabled installed extension's repository (`Repository.Discover` walks up, so a monorepo is found too) and reloads - and EasyBIM Auto Update calls that same routine. The first draft (generated wrapper buttons that `exec` the source script, clones outside pyRevit's roots) would never have been updated, worked only for Python bundles, and skipped the source's `startup.py`/hooks; junctions and folder copies lose the source's `lib/` from the search path | No wrapper, no exec, no `__commandpath__` patching; every bundle type works; the same object means enabled state, icon and tooltip stay in sync with no bindings - and a placed button therefore keeps its own title (renaming = v1.5, a cloned `RibbonButton` with WPF bindings). My Ribbon runs no updater; `test_my_ribbon_command_names` fails the build if `git_pull`/`update_pyrevit` appears in it | Active |
| 2026-08-15 | A placement records pyRevit's `control_id` **and** a path of `{name, title}` pairs; the engine matches by exact Id first, then walks tab > panel > (container >) button matching each level by folder name *or* title | pyRevit names the live `RibbonItem.Id` from the **folder** for tabs and buttons but from the **title** for panels and pull-downs (`create_ribbon_panel(panel.ui_title)`, `PushButtonData(button.name, ...)`, verified in the release branch), so `control_id` is only nearly the live Id whenever a panel or pull-down title differs from its folder. Stacks have no ribbon object (AdWindows renders a `RibbonRowPanel`), so their level is left out of the path and the walk looks through row panels | Upstream renames of a title still resolve by Id; a moved button reports "missing" with the level that failed rather than being guessed; pinned by `test_my_ribbon.py` (structural match with newline/title differences, Id wins over a renamed title) | Active |
| 2026-08-15 | The My Ribbon apply runs from the first Idling tick of every session, queued by `startup.py`, **before** the auto-update consumer; the objects it added last time are mirrored in a pyRevit envvar and taken back first | `startup.py` runs while later extensions are still loading, so their tabs are not on the ribbon yet; the auto-update can end in a pyRevit reload whose own `startup.py` queues the apply again, so nothing runs inside an engine that reload is disposing, and the user sees their ribbon before any network work | Idempotent apply (clear ours, add in registry order, hide tabs, drop empty panels/tabs we created); a registry with nothing to do and no leftovers never touches the ribbon; the pyRevit tab and every destination tab are never hidden (`PROTECTED_TABS`); pinned by `test_idling_dispatcher` (order) and `test_my_ribbon` (idempotence, take-back, protection) | Active |
| 2026-08-15 | The registry `pyRevit_EasyBIM_MyRibbon.json` (`get_universal_data_file`, beside-and-swap, format-versioned) is the single source of truth; the manager stages into a working copy and only Apply saves, deletes and applies; a source's folder is deleted only if `installed_by_my_ribbon` and only under My Ribbon's own folders | Same rules as Tag Align presets and Sheet Manager staging: one Apply, one place the truth lives, no half-written file, a newer format refused rather than half-read; deleting a folder the user installed themselves would be the single worst outcome | Export = the same document (no machine paths, never a credential); Import previews Merge/Replace and downloads what the computer lacks; `test_my_ribbon_command_names` pins the one `save_registry` call site, the guard before `remove_tree`, and that `reload_pyrevit` is called once, from the main loop, after the window closed | Active |
| 2026-08-16 | My Ribbon tab visibility is one registry list, `hidden_tabs`; the engine's `PROTECTED_TABS` moved from `("pyrevit",)` to `("easybim", "modify")` (**supersedes** the 2026-08-15 row that protected pyRevit), and Apply closes the window | The user asked for a Show/Hide window over every tab with EasyBIM frozen, and confirmed freezing Modify (Revit's contextual editing tab) and letting the pyRevit tab be hidden with a warning. One list is the only way the per-source checkbox and the whole-ribbon window can never disagree; files from round 1 are read by unioning the per-source flags into it. EasyBIM frozen *is* the recovery path, so pyRevit no longer needs to be | `replace_hidden_tabs` re-derives the per-source flags; removing a source un-hides only tabs hidden because of it; a destination tab is never hidden; a tab invisible for another reason is shown disabled so Apply never forces it on; pinned by `test_my_ribbon` (EasyBIM/Modify never hidden, pyRevit hidden, hidden_tabs union) and `test_my_ribbon_state` (flag/list interplay) | Active |
| 2026-08-16 | Remove keeps files; **Uninstall** is a separate button that stages the deletion and is enabled only for what My Ribbon installed itself (a downloaded extension, a shared repository clone when no other source uses it, a Dynamo bundle) | Round 1's Remove quietly staged a folder deletion when `installed_by_my_ribbon` was set; the user wanted the two acts apart, and installed extensions, native tabs and add-ins can never be uninstalled from here | A Dynamo button is nothing but the bundle My Ribbon wrote, so Remove deletes that one too; pinned by `test_my_ribbon_command_names` (`pending_deletes.append` once in Remove, guarded by kind; Uninstall checks `installed_by_my_ribbon`) | Active |
| 2026-08-16 | Buttons of Revit's own tabs and other add-ins are a source kind `ribbon`, read live from AdWindows (`describe_ribbon_tab`) by **type name**: buttons and toggles are buttons, split/menu/list/checklist/radio groups are drop-downs with children, `RibbonRowPanel` stacks are looked through, separators skipped, everything else (galleries, combos, text boxes, sliders, labels) refused as `ribbon-<type>` | The shared-object mechanism does not care who made the button, and the user confirmed wanting native and add-in buttons on their panels; a gallery or combo shared onto another panel is a control with no home and is not worth the risk | Picker rows carry the live `Image` as an ImageSource (`_small_icon(path, source)`); placements record `control_id = item.Id` and a path of Id/Title pairs; such sources can only be Removed; pinned by `test_my_ribbon.LiveTabTests` | Active |
| 2026-08-16 | A Dynamo graph becomes a **real pyRevit bundle** in My Ribbon's own extension (`EasyBIM_MyRibbon.extension\My Ribbon Library.tab\Dynamo.panel\<Title>.pushbutton`) whose `bundle.yaml` sets `engine: {automate: true, dynamo_path: <original path>}`; `script.dyn` is a copy refreshed on Apply; the library tab is hidden; the placed item gets Revit's own Dynamo images copied onto it at apply time | `DynamoBIMEngine.cs` takes `dynPath` from `ExecEngineConfigs.dynamo_path` when set, so the ORIGINAL graph runs and the user's edits count next time (confirmed choice: run where it lives, export the path only); pyRevit still needs `script.dyn` to recognise the bundle, hence the copy; Revit's own Dynamo images give the "same as native" icon the user asked for without shipping Autodesk's artwork; a drawn look-alike is the fallback and the user may pick a PNG | `.dyf`/`.py`/non-graphs refused with a sentence; facts read from the file (1.x XML vs 2.x JSON, Python-node engines, package dependencies) go into the window and the tooltip; a bundle is never written without a graph; a missing original keeps the last copy running and reads "graph file missing" with **Locate graph...**; pinned by `test_my_ribbon_host.DynamoBundleTests`, `test_my_ribbon_state.DynamoHelperTests`, `test_my_ribbon.DynamoIconTests` | Active |
| 2026-08-19 | Family Types shows the family's type table as a grid - **types are rows, parameters are columns**, the type-catalogue shape - and **instance parameters are columns too**, marked `(default)` | A `FamilyType` stores a value for *every* family parameter, instance ones included; that per-type value is the default a new instance is placed with and it genuinely differs between types, which is exactly what Revit's own dialog means by "(default)". `families_downgrade_export.read_types` already depends on this - it reads `manager.GetParameters()` (all of them) and calls `family_type.AsDouble(parameter)` per type - so filtering them out would drop real data. Revit 2022+ stacks its own dialog the other way, but a transposed export would stop being a catalogue, and the export is half the point | Values are display strings (`AsValueString` out, `SetValueString` back) so the grid reads like the rest of Revit and parses in the user's units. Materials stay editable because they are unique by name inside a family document; any other ElementId reference is shown and locked with the reason in its tooltip, alongside formula-driven, reporting, read-only and non-user-modifiable parameters | Active |
| 2026-08-19 | A new family type is **seeded from an existing type**, and deletes and renames are applied **before** creates | `FamilyManager.NewType` copies the *current* type's values into the type it creates, so a row shown blank in the grid would come out of Revit carrying whatever the current type held - the grid and the family would disagree about a type the user just made. Seeding is also what Revit's own New does. The ordering is the swap case: both a delete and a rename free a name, so creating first would collide when "A" is renamed to "B" while a new "A" appears | `compute_staged_changes` returns four buckets and `apply_changes` walks them in that order, pinned by `test_family_types_command_names`. Two things Revit will not do are reported rather than faked: the last surviving type refuses to be deleted, and a cleared cell is skipped with a note because there is no way to unset a family parameter - writing `0` would be a lie | Active |
| 2026-08-19 | Family Types **asks the user** how to reload, via the shared *Family Already Exists* prompt, rather than fixing `overwriteParameterValues` | Load Parameters fixes it to `False` (2026-08-03) because its job - add a parameter and put the family back - must not touch the project's existing type values. Here changing those values *is* the job, so the answer is not ours to pick. The 2026-08-03 row explicitly warned that sharing one load-options class would make one of the two silently drift; the difference is now in the *answer*, not in the class | The prompt and `IFamilyLoadOptions` moved to `lib/easybim/family_load_options.py` for their second consumer, the same move the 2026-08-15 row made for `family_selection_*`. Families Transfer keeps `FamilyTransferLoadOptions` as an alias so its call sites read unchanged; the four assertions that pinned the prompt to its `script.py` now point at the lib module, and `test_families_transfer_links` pre-registers the new lib module before `exec_module` | Active |
| 2026-08-19 | The Excel header cell carries the parameter name on line 1 and its **ElementId on line 2, in the same cell**; rows match by type name | The id is the round-trip key because it survives a parameter rename in Revit, which the header text does not - and it is on the *visible* sheet rather than only in `_metadata` (as Sheet Manager's ids are) so a reader can see what an import matched on. Rows match by name because that is the type-catalogue contract; through a spreadsheet a rename and a new type are indistinguishable, so renaming belongs in the grid where they are not | `text_wrap` is load-bearing - without it Excel clips the second line. Column resolution is id, then name, then the `_metadata` key by position, and anything left over is reported rather than guessed at. Import is staged into the grid, never written: the state module is asserted to contain no `Transaction` at all, and Apply stays the only door | Active |
| 2026-08-19 | A family document the user **already had open** is never closed by Family Types | `EditFamily` on a family that is already being edited hands back the document the user is working in (recorded in `load_parameters_revit.open_family_titles`), and `Close(False)` on it would end their edit session without warning | The launcher records whether the document was open before it called `EditFamily` and closes only what it opened; pinned by `test_family_types_command_names`. Neither the launcher nor the Revit layer ever calls `Save`/`SaveAs` - the family editor case leaves saving to the user, and the project case reloads instead | Active |
| 2026-08-19 | **Correction.** Family Types must NOT gate editability on `Parameter.IsReadOnly` or `Parameter.UserModifiable`; only formula-driven, reporting and non-material element references lock a cell | `FamilyParameter` derives from `Parameter` and inherits both flags, but they describe whether the base class's `Parameter.Set()` would succeed - and a family parameter is never written that way, it is written through `FamilyManager.Set(familyParameter, value)`, a different door. Gating on them locked most of the table on the first real family, which is what the user reported. The flags were copied from `families_downgrade_export._parameter_record`, which reads them only to *describe* a parameter in a manifest; that meaning does not transfer to "can the user edit this". Yes/no parameters were collateral - Integer storage caught by the same gate, nothing yes/no-specific was ever wrong | The tool now errs toward offering the edit: `_set_value` already records a per-cell reason and `apply_changes` commits the rest, so being wrong in this direction costs one reported line instead of an unreachable column. `test_family_types_{state,command_names}` both assert the two flags never come back. **The general lesson: a flag copied from code that only describes something is not automatically a flag you can decide with.** | Active |
| 2026-08-19 | A yes/no column stores a **bool** in the row, not `Yes`/`No` text, and renders as a checkbox; the workbook format is unchanged | A CheckBox binds to a bool, and a converter in IronPython is more machinery than the value is worth. `cell_value` and `display_text` are the single seam: Revit hands over a bool, an import hands over text, the grid hands over either, and one pair of functions decides so the three never disagree | `build_export_matrix` still writes `Yes`/`No`, so a workbook from the previous build imports unchanged and a new export is comparable with an old one - pinned by a test that flips a yes/no cell through a real workbook and back | Active |
| 2026-08-19 | The grid selects **cells** (`SelectionUnit="CellOrRowHeader"`), not rows - the repo's first - and a bulk edit is "every selected cell in this column" | The user asked to drag or shift-select cells down a column and set them at once. Every other grid in the repo is `FullRow` and propagates across selected *rows* (`sheet_manager_state.propagate_edit`); cell selection is what was actually asked for, and Excel-like | Two consequences. `RowHeaderWidth` had to come back from 0 so a whole row is still selectable, because the New / Duplicate / Delete Type buttons read that selection - `_selected_rows` now derives from `SelectedCells`, so any cell in a row counts. And the **`PreviewMouseLeftButtonDown` guard is load-bearing**: WPF collapses a multi-selection on mouse-down *before* the click or the edit is reported, so without it every bulk gesture would silently touch one cell. Sheet Manager hit the same wall (`sheet_manager_ui.py:237-247`) and this copies its answer | Active |
| 2026-08-19 | The type/instance separator is a hand-drawn `Canvas` band above the column headers, and it **hides itself rather than failing** | A WPF `DataGrid` cannot merge header cells - there is no precedent anywhere in this repo for a spanning, banded or multi-row header - so the band is positioned by hand from the columns' `ActualWidth`, offset by the scroll position and clipped past the frozen first column | Every step is guarded and any exception clears the canvas: a band that cannot line up must never take the grid down with it. Driven by `ScrollChanged` plus a `LayoutUpdated` pass gated on a cheap width/count signature, because `LayoutUpdated` fires on every render and `_band_segments` walks every column. Column rebuilds clear both signatures or the cheap one short-circuits the redraw | Active |
| 2026-08-19 | **Correction.** Yes/No detection must not rest on recognising a spec. `_is_yes_no` now tries four independent routes - the `SpecTypeId.Boolean.YesNo` object, the spec id as text, the legacy `ParameterType` enum, and finally **how Revit renders the value** | The first two builds both shipped a single spec-string test and both rendered Yes/No columns as 0 and 1 on the user's Revit, because the parameter fell through to the plain-Integer path. The failure could not be reproduced off Revit - a fake answering `autodesk.spec.boolean:yesNo-1.0.0` satisfies the old code - so the fix is not "repair the spec test" but "add a route that cannot depend on knowing the spec": an Integer whose value is 0 or 1 and whose `AsValueString` carries no digit is a checkbox, which holds in any language and on any release | `_data_spec` centralises `GetDataType` (2022+) / `GetSpecTypeId` (2021) so `_is_yes_no`, `_is_material` and `_spec_label` stop each rolling their own. New `test_family_types_revit.py` drives all of it against fakes shaped like each API generation, and is mutation-checked: restoring the shipped detection fails exactly the two fallback tests. **The lesson: when a probe cannot be reproduced off the host, add an independent route rather than a better guess at the same one.** | Active |
| 2026-08-21 | `View Align` grew an Align Title Block option, and the title block collector moved to `lib/easybim/sheet_titleblocks.py` | The tool aligns viewports to *absolute sheet coordinates*, so two sheets whose title blocks sit at different origins end up with every drawing at the right sheet coordinate and the wrong title-block coordinate - the same failure the 2026-08-04 row records for `Linked Sheets Transfer`, which already moves the host title block for exactly this reason. A second copy of that would let the two tools drift | The nested sub-option carries the rest of the sheet with the frame but never the viewports this run is aligning, whose view titles ride along on `LabelOffset`; unselected viewports do move, so they keep their place inside the frame. A sheet with no title block is a **note**, not an issue - `stats.issues` aborts the entire run, and one frameless sheet must not cost fifty others their alignment. There is no `ElementTransformUtils.CanMoveElement` to pre-test with (the `Can*` pair is `CanMirrorElement`/`CanMirrorElements`), so elements move one at a time and an immovable one costs a note rather than a rollback. The move is sheet-space only, so it escapes the stale-`View.Outline` trap the 2026-08-04 row pins on the crop and scope box options | Active |

## Session Handoff Log

| Date | What Changed | Files Touched | Checks Run | Next Step |
|------|---------------|---------------|------------|-----------|
| 2026-08-20 | Added Circuit Schedule: a right-docked System-Browser-style pane for electrical circuits only. The whole distribution chain is one recursive tree - the board nothing feeds at the root, every sub-panel under the circuit that feeds it, devices as leaves - built from `BaseEquipment` + `Elements` in a single collector pass. Boards, circuits and loads are styled apart (bold board naming its feeder / numbered circuit carrying its readings / small grey leaf) so a feeder to a sub-board never reads like a branch circuit, with a depth colour ramp and a breadcrumb of the selected row's upstream path. Search prunes to matching branches keeping ancestors; circuit numbers match by token so `12` does not find `112`. Guards for loops, boards fed twice, spares and circuits with no panel. Double-click selects and zooms through an ExternalEvent; Refresh keeps expansion and search | `lib/easybim/circuit_schedule_{state,revit,panel}.py` (new), `lib/easybim/ui/circuit_schedule_panel{,_window}.xaml` (new), `EasyBIM.tab/Misc Tools.panel/Circuiting.pulldown/Circuit Schedule.pushbutton/{script.py,bundle.yaml,icon.png,icon.dark.png}` (new), `Circuiting.pulldown/bundle.yaml`, `startup.py`, `Development Space/tests/test_circuit_schedule_{state,command_names}.py` (new), `README.md`, `AGENTS.md` | `python3 -m unittest discover -s "Development Space/tests"` (1530 passed, 19 skipped; was 1486), `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`, both XAML parsed and their `x:Name`/handler sets asserted identical | In-Revit: **restart Revit** (a new dockable pane is only registered during app init - a pyRevit reload silently takes the floating-window fallback); open a model with two levels of distribution and confirm the root is the switchboard; expand to a receptacle and confirm a sub-board reads unlike a device; search a circuit number and confirm the upstream chain survives and `12` does not surface `112`; double-click a load and a board; read the path line from a deep row; Expand/Collapse All, then Refresh and confirm expansion and search survive; switch documents and press Show, then Refresh; open a model with no circuits; check both Revit themes |
| 2026-03-02 | Created baseline AGENTS governance file | `AGENTS.md` | None | Add new decision/handoff rows after merged workflow changes |
| 2026-03-04 | Added Git Worktree Manager Style governance and mirrored rule intent for skills | `AGENTS.md`, `C:\Users\RML\.codex\skills\rml-repo-easybim-extension\SKILL.md` | `rg` keyword checks, focused `git diff`, `git status --short` scope check | Continue applying these defaults for all EasyBIM worktree operations |
| 2026-03-04 | Added documented standard git promotion flow and linked `skills.md` process | `AGENTS.md`, `skills.md` | Doc-only update | Follow this process for routine feature promotion |
| 2026-03-04 | Added shorthand workflow command definition and default semantics | `AGENTS.md`, `skills.md` | Doc-only update | Superseded by `sync worktrees` wording |
| 2026-03-04 | Added explicit-name worktree/branch parity rule and removed auto-suffix creation behavior | `AGENTS.md`, `C:\Users\RML\.codex\skills\rml-repo-easybim-extension\SKILL.md` | Worktree recreate + doc sync + status verification | Keep explicit worktree names and branch names identical when requested |
| 2026-03-04 | Standardized primary-worktree 3-stage promotion flow via `Temp-Phase-and-View-2` before `main` | `AGENTS.md`, `skills.md`, `C:\Users\RML\.codex\skills\rml-repo-easybim-extension\SKILL.md` | Doc update for workflow governance | Superseded by creator-branch sync default |
| 2026-03-13 | Replaced fixed staging-branch shorthand with creator-branch `sync worktrees` semantics | `AGENTS.md`, `skills.md`, `C:\Users\RML\.codex\skills\rml-repo-easybim-extension\SKILL.md` | Targeted doc sync across root repo, mirrored skill, and worktree copies | Keep `main` promotion as a separate explicit step |
| 2026-08-02 | Added Sheet Manager bundle (staged grid editor: per-revision checkbox columns, sheet/TB param editing, load sheet list/print set, revision+parameter filters, sort, copy sheet info, search & replace, save print set, Excel round-trip) and renamed `Print.panel` to `Sheet.panel` | `EasyBIM.tab/Sheet.panel/Sheet Manager.pushbutton/*`, `lib/easybim/sheet_revisions.py`, `Development Space/tests/test_sheet_manager_*.py`, `test_sheet_revisions.py`, `test_print_set_command_names.py`, `AGENTS.md` | `python -m pytest "Development Space/tests"` (277 passed), `python -m compileall EasyBIM.tab lib`, XAML parse tests | In-Revit verification of Phase-1 exit criteria (XamlReader templates + Reactive rows), then user feedback on pinned-element handling |
| 2026-08-02 | Reshaped Batch Transfer View Template Settings into the `View Template` pushbutton: view-or-template source radios (active view preselected), view-or-template target radios with template-locked views greyed, `Transfer All`, and a `Selective Transfer` dialog with per-item V/G Overrides edit windows (read-only source values, all selections off by default); post-review hardening: RVT Links V/G group gated to Revit 2024+ (2023 shows an unsupported hint), indeterminate Include click selects all instead of clearing, V/G rows detected via `VIS_GRAPHICS_*` BuiltInParameter ids as localization fallback, transfer summary zeroed on group rollback and item notes split from skipped-target counts, target count refreshes on direct checkbox clicks | `EasyBIM.tab/Views.panel/View Template.pushbutton/*`, `lib/easybim/view_template_ribbon.py`, `Development Space/tests/test_view_template_transfer_state.py`, `test_view_template_ribbon.py`, `AGENTS.md` | `python -m pytest "Development Space/tests"` (304 passed), `python -m compileall EasyBIM.tab lib`, XAML XML parse check, adversarial multi-agent review (9 findings confirmed and fixed or gated) | In-Revit smoke test: ribbon icon aliases, XAML load, tri-state Include cycle, `CreateViewTemplate` return/rollback behavior, selective apply to plain views, `GetLinkOverrides` id kind, import-category enumeration, partial-group writes propagating through template-controlled views |
| 2026-08-02 | Added Clash Detection Mode: live forward-only interference checking. Native-Interference-Check-shaped setup window with Extended multi-select category lists (shift/drag/spacebar bulk ticking), `Start Ongoing Detection Mode` replacing OK, host + linked-model `Categories from` sources, Silent Mode driving a right-side dockable pane, non-modal alert window otherwise, `Show` on every row, `Stop Detection` on both surfaces. Engine subscribes `DocumentChanged` (set unions only) + `Idling` (0.35s debounce, 25-element/40ms budget), quick-filters before exact filters, solid-transform path for links, bounded re-sweep on link-instance moves, memory-only state with hard caps | `EasyBIM.tab/Misc Tools.panel/Clash Detection Mode.pushbutton/*`, `lib/easybim/clash_detection_{state,engine,panel,alert}.py`, `lib/easybim/wpf_notify.py`, `lib/easybim/ui/clash_detection_*.xaml`, `startup.py`, `hooks/doc-closing.py`, `Development Space/tests/test_clash_detection_*.py`, `Development Space/docs/superpowers/specs/2026-08-02-clash-detection-mode-design.md`, `AGENTS.md` | `python -m pytest "Development Space/tests"` (409 passed), `python -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML XML parse checks | In-Revit smoke test: pyRevit `register_dockable_panel` availability and right-side docking, `__persistentengine__` keeping the delegates alive, `INotifyPropertyChanged` bulk-tick refresh, link solid-transform accuracy, `Selection.SetReferences` on 2023+ vs the zoom fallback, and responsiveness while moving ~500 elements |
| 2026-08-02 | Clash Detection round 2. Fixed the bug that lost clashes created by a move or a copy (empty query result read as "resolved"; a pair could be deleted in the pass that found it) and the silent drop of grouped/pasted containers. Added Pause/Resume with pair re-validation, a status window on the ribbon button, a green/amber ribbon badge, Edit Categories from the panel, row checkboxes with one Show button acting on every ticked clash, and raised `MAX_QUEUE` to 20000 so a large paste is checked end to end | `lib/easybim/clash_detection_{state,engine,panel,alert,setup,status,ribbon,revit}.py`, `lib/easybim/ui/clash_detection_*.xaml`, `EasyBIM.tab/Misc Tools.panel/Clash Detection Mode.pushbutton/script.py`, `Development Space/tests/test_clash_detection_*.py`, `Development Space/docs/superpowers/specs/2026-08-02-clash-detection-mode-design.md`, `AGENTS.md` | `python -m pytest "Development Space/tests"` (448 passed), `python -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML XML parse checks | In-Revit: confirm a move and a multi-element paste now report and keep their clashes (watch the `resolved` counter - it must not tick up on a move that created one), grouped/pasted groups detected, badge colours, Resume re-check, Edit Categories keeping in-scope clashes, and ticked-rows Show framing them together |
| 2026-08-03 | Clash Detection round 3. Fixed the reason the mode could not be stopped once the panel was closed: the running flag was a bare module global, so a recycled pyRevit engine reported the mode off while its handlers stayed attached. Merged the status window into the main window, which the ribbon button now always opens and which carries Open Panel / Pause / Resume / Stop plus live state. Gave every clash a checkbox per element instead of one per pair. Reworded the main description to "Watch for clashes created while you keep modeling." and dropped the "Tick at least one category" hint | `lib/easybim/clash_detection_{engine,state,panel,alert,setup}.py`, `lib/easybim/ui/clash_detection_{setup,panel,panel_window,alert}.xaml`, `EasyBIM.tab/Misc Tools.panel/Clash Detection Mode.pushbutton/{script.py,bundle.yaml}`, removed `lib/easybim/clash_detection_status.py` + xaml, `Development Space/tests/test_clash_detection_*.py`, spec, `AGENTS.md` | `python -m pytest "Development Space/tests"` (450 passed), `python -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML parse + handler-resolution checks | In-Revit: close the panel and confirm the ribbon button reopens it and can Stop; tick individual elements and confirm Show frames exactly those; confirm the session strip appears whenever a session is running, including after a pyRevit reload |
| 2026-08-03 | Fixed why an extension update showed no UI change: `__persistentengine__` (this repo's only use, added for Clash Detection) keeps the engine's `sys.modules` across a pyRevit reload, so updated modules were never re-imported. The command now drops its own modules on launch when no session is live, and the panel mirrors its view/row collection through pyRevit envvars so a re-imported module keeps driving the pane Revit already built. Added a `Build <timestamp>` stamp on the main window, read from file mtimes, so "did my update land?" is answerable at a glance | `EasyBIM.tab/Misc Tools.panel/Clash Detection Mode.pushbutton/script.py`, `lib/easybim/clash_detection_{setup,panel}.py`, `lib/easybim/ui/clash_detection_setup.xaml`, `README.md`, `Development Space/tests/test_clash_detection_no_local_files.py`, `AGENTS.md` | `python -m pytest "Development Space/tests"` (452 passed), `python -m compileall "EasyBIM.tab" lib` | In-Revit: pull the update, reload pyRevit (no restart), open the tool and confirm the Build stamp matches the pull and the session strip/description are present |
| 2026-08-03 | Added the `Tag Align` bundle to `Misc Tools`: reference-tag capture (one tag with an "any orientation?" follow-up page, or several matched per element type and orientation), two scope radios with paired-family as the default, a blocking conflict window with a winner per group and a one-click switch to exact-type matching, then click-one-at-a-time (Esc ends) or a batch bar with Filter / Select / Deselect / Process. Offsets are stored in both the view frame and the element frame plus the source view scale, so they survive rotation and a change of view scale. Leader elbow and free end are copied offset-relative; pinned, multi-reference, linked and already-aligned tags are skipped with named reasons and a "Select skipped elements" button | `EasyBIM.tab/Misc Tools.panel/Tag Align.pushbutton/*` (script, `tag_align_{state,revit,ui}.py`, 8 XAML, bundle, icons), `Development Space/tests/test_tag_align_state.py`, `test_tag_align_command_names.py`, `Development Space/docs/superpowers/specs/2026-08-03-tag-align-design.md`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (565 passed), `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML parse + per-window handler/control resolution checks | In-Revit: confirm the greyed-but-responsive Align buttons, the rotated-pipe case under "any orientation = yes", a blocked conflict from two references on one door type, the scope radio alone producing `SCOPE_AMBIGUITY`, single-undo batch rollback, and a 1:100 reference reproducing its printed distance in a 1:50 view |
| 2026-08-03 | Tag Align scope reshaped into a three-rung ladder: added `All the families in the same category` as the first radio and the new default, above paired-family and exact-type. Resolution already preferred the narrowest match, so the wider default only adds a fallback; validation now groups every reference together under category scope, so two families at one orientation collide as they should. The conflict window's button narrows one step (`Narrow to: ...`) instead of jumping straight to exact type | `EasyBIM.tab/Misc Tools.panel/Tag Align.pushbutton/{tag_align_state.py,tag_align_ui.py,script.py,TagAlignWindow.xaml,ReferenceConflictWindow.xaml}`, `Development Space/tests/test_tag_align_{state,command_names}.py`, spec, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (571 passed), `python3 -m compileall "EasyBIM.tab" lib` | In-Revit: with one reference on a door, confirm every family in Doors now aligns; add a second reference on another family at the same orientation and confirm the conflict names "(any family)" and that Narrow to: paired family clears it |
| 2026-08-03 | Added the `Circuiting` pulldown to `Misc Tools` with its first command, `Update Circuit Rating`: pick a current parameter carried by the circuited elements, tick the target circuit parameters (every writable numeric parameter on the circuits; native Rating and Frame preselected), review the circuits in a checkbox grid with All/None, then Update. The highest reading across a circuit's elements wins, the donor element and the targets' existing values are shown per row, unchanged rows are marked, and circuits no element carries a value for are dropped from the list and counted as skipped. One model scan feeds every source/target switch; the write is one transaction | `EasyBIM.tab/Misc Tools.panel/Circuiting.pulldown/*` (pulldown bundle + icons, `Update Circuit Rating.pushbutton/` with `script.py`, `circuit_rating_{state,revit,ui}.py`, `UpdateCircuitRatingWindow.xaml`, bundle, icons), `Development Space/tests/test_circuit_rating_state.py`, `test_circuit_rating_command_names.py`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (668 passed), `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML parse + handler/control resolution checks | In-Revit: confirm the pulldown and both icon themes; confirm a circuit whose elements all lack the parameter never appears and is counted as skipped; confirm a mixed circuit takes the highest and names the donor; untick Frame and confirm only Rating is written; confirm one Ctrl+Z reverts the whole run |
| 2026-08-03 | Added the zero-amp report to Update Circuit Rating: every run now ends with a read-only WPF table of the circuits still at zero amp (Panel Name / Circuit Number / Circuit Name), read back from the committed model and covering every circuit in the project. The success summary moved into that window; the alert is now raised only when a write failed, so a clean run shows one dialog instead of two | `EasyBIM.tab/Misc Tools.panel/Circuiting.pulldown/Update Circuit Rating.pushbutton/{script.py,circuit_rating_state.py,circuit_rating_revit.py,circuit_rating_ui.py,ZeroRatingReportWindow.xaml}`, `Development Space/tests/test_circuit_rating_{state,command_names}.py`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (678 passed), `python3 -m compileall "EasyBIM.tab" lib`, XAML parse + handler/control resolution checks | In-Revit: run with one circuit left unticked and confirm it appears in the report; untick Frame and confirm a circuit with Rating set but Frame zero drops out of the report; confirm the report is empty-stated when every circuit is rated |
| 2026-08-03 | Added Save Settings / Load Previous Settings to Tag Align. Matching moved from ElementIds to names (category signature + family + type) so a preset loads into any model. Three destinations: roaming AppData, Extensible Storage inside the .rvt (the ACC answer - syncs to central with the model), and a user-set shared folder (network drive or ACC Desktop Connector). Load picker lists all sources with an auto-written `Last used` pinned first, plus Rename and Delete. Also fixed a latent bug: an inactive `FamilySymbol` is now activated before `IndependentTag.Create`, which a loaded preset makes far more likely to hit | `EasyBIM.tab/Misc Tools.panel/Tag Align.pushbutton/{tag_align_presets.py,tag_align_state.py,tag_align_revit.py,tag_align_ui.py,script.py,SavePresetWindow.xaml,LoadPresetWindow.xaml,TagAlignWindow.xaml}`, `Development Space/tests/test_tag_align_presets.py` (new), `test_tag_align_{state,command_names}.py`, spec, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (625 passed), `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML parse + per-window handler/control checks | In-Revit: save locally, restart Revit, load; load the same preset in another project and in one missing a family; rename a type and confirm the old preset reports it unresolved; save into an ACC cloud model, Sync to Central, and confirm a second user sees it; save to a Desktop Connector folder; try in-model on a read-only model and confirm the failure is explained; never press Save and confirm `Last used` appears |
| 2026-08-03 | Added the `Load Parameters` bundle to `Parameters`: one window that adds shared parameters to families and/or binds them to project categories, staying open after a load so one selection serves both. Left half is a parameter `DataGrid` (checkbox, name, data type, source, Group and Type/Instance combos) with shift-select, a bulk bar and combo-edit propagation across the selection; right half is a Families/Categories `TabControl` whose lists carry the Extended-selection checkbox pattern. Families come from the project or a scanned folder (backups and workshared files dropped, authoring version read via `BasicFileInfo`); categories pre-tick from the checked families. Every run is a dry-run plan, a confirmation with acknowledgement ticks for the `.rfa` upgrade and any Instance/Type flip, a cancellable progress bar, then a two-bucket report | `EasyBIM.tab/Parameters.panel/Load Parameters.pushbutton/*` (script, `load_parameters_{state,revit,ui}.py`, 4 XAML, bundle, both icons), `EasyBIM.tab/Parameters.panel/bundle.yaml`, `Development Space/tests/test_load_parameters_{state,command_names}.py`, `Development Space/docs/superpowers/specs/2026-08-03-load-parameters-design.md`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (756 passed), `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML parse + per-window handler/x:Name resolution checks, cell-fragment render check | In-Revit: confirm the button and both icon variants; shift-select four rows and change one Group combo (all four move); tick families and watch their categories tick; `Load to Families` on 3 project families then **one** Ctrl+Z; press `Load to Project` from the still-open window; a parameter bound to Doors then loaded for Windows must end up on *both*; scan a folder holding a `.0001.rfa` and an older-Revit family and confirm the backup is absent and the upgrade tick is demanded; cancel mid-run in each phase; verify `SharedParametersFilename` is unchanged afterwards and no `pyRevit_EasyBIM_LoadParameters.txt` is left behind |
| 2026-08-04 | Added the `Linked Sheets Copy` bundle to `Sheet`: pick a loaded Revit link, tick its sheets, and each is recreated here with a matching plan view per viewport, the link set to **By Linked View**, and every viewport placed by **model coordinates** so the grids land where they do on the linked sheet. Level mapping is driven by Copy/Monitor with a name/elevation fallback and an editable dropdown; sheet-number clashes (against the model and within the batch) are flagged red and block the run, with prefix/suffix boxes to clear them. Every placement is verified at a second point out at the crop corner and the miss is reported in printed millimetres. Extracted `View Align`'s anchor solver into `lib/easybim/sheet_geometry.py` and switched View Align over to it | `EasyBIM.tab/Sheet.panel/Linked Sheets Copy.pushbutton/*` (script, `linked_sheets_copy_{state,revit,ui}.py`, 3 XAML, bundle, both icons), `EasyBIM.tab/Sheet.panel/bundle.yaml` (new panel layout), `lib/easybim/sheet_geometry.py` (new), `EasyBIM.tab/Views.panel/View Align.pushbutton/script.py`, `Development Space/tests/test_sheet_geometry.py`, `test_linked_sheets_copy_{state,command_names}.py`, `Development Space/docs/superpowers/specs/2026-08-04-linked-sheets-copy-design.md`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (892 passed), `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`, XAML parse + per-window handler/`x:Name` resolution checks | In-Revit: print a new sheet over its linked original and confirm the grids coincide (report deviation ~0.00 mm); repeat on a rotated link and on one placed by shared coordinates; a 1:50 and a 1:100 view; a shape-edited crop and a crop switched off; a sheet carrying a section and a legend (sheet built, both named as skipped); a linked level with no Copy/Monitor counterpart left unmapped; check V/G -> RVT Links reads By Linked View with the right view; one Ctrl+Z reverts the run; re-run the same selection and confirm the number clashes are caught |
| 2026-08-10 | Families Transfer: renamed `Load More from Project` to `Load More from Recent Project`; moved the model-pick button off the source page onto the family browser as `Select More in the model` (the browser keeps its `Transferable Families` title); added a third source card, `Load More from Revit Links`, that reads families out of loaded links without opening the `.rvt`. Added the missing `test_families_transfer_command_names.py` and a fakes-driven `test_families_transfer_links.py`. Perf: family scans cached across step re-entries, `resolve_family`'s per-family collector pass replaced by one index per document, the duplicate eligibility check dropped, Select All/None no longer rebuilds the list, and transfer/export run behind a cancellable progress bar. Extracted `lib/easybim/copy_paste.py`. | `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/*`, `lib/easybim/copy_paste.py`, `EasyBIM.tab/Sheet.panel/Linked Sheets Copy.pushbutton/linked_sheets_copy_revit.py`, `Development Space/tests/test_families_transfer_{state,command_names,links}.py`, `Development Space/tests/test_linked_sheets_copy_command_names.py`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (959 passed); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; pyflakes clean; XAML parse + per-window handler/`x:Name` resolution (now part of the suite) | **Unverified in Revit, and the feature turns on it: does `link_doc.EditFamily()` succeed or throw?** Probe on a scratch model first, and check whether a half-succeeded `EditFamily` leaves the link needing a reload. Then smoke-test end to end: link a model, tick the link, browse, transfer into a second open project, cancel mid-run. Also confirm `Family.IsEditable` inside a link. |
| 2026-08-11 | Families Transfer refinement round: all three source cards rebuilt to one shape (15pt bold solid headers, one `X selected, Y unchecked.` count, Select All/None everywhere, Load More on the header row); every star row given a `MinHeight` so only the lists shrink when the window is dragged; card 1 renamed `Selection from Recent Project` and its 20pt count dropped; Revit links moved to a new `LinkSelectionWindow` page so card 3 lists chosen link families; `Hide Un-checked` added to both second pages; `Select More in the model` renamed `Select in the model`; tags and annotation families made pickable. | `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/*` (incl. new `LinkSelectionWindow.xaml`), `Development Space/tests/test_families_transfer_{state,command_names,links}.py` | `python3 -m pytest "Development Space/tests"` (982 passed); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; pyflakes clean; XAML parse + per-window handler/`x:Name` resolution, now including a lowercase-`x:Name` check the `^[A-Z]` filter used to miss | **Nothing here is verified in Revit.** New this round: drag the window and confirm only the lists shrink; check the three headers read as one hierarchy; click a tag with `Select in the model` and confirm its family lands; walk card 3 -> links -> families -> Add and confirm the families come back ticked, then untick one on page 1 and confirm it drops. Still open from last round: does `link_doc.EditFamily()` succeed or throw - probe on a scratch model. Also unresolved: `MinHeight="720"` does not fit a 1080p screen at 150% scaling; if it bites, drop the card action buttons into their header rows. |
| 2026-08-12 | Two fixes. Families Transfer: the footer said "N active-project families selected." under three source cards; the window now owns the number and reports the total across all three, and `source_status`/`status_text` are gone. Load Parameters: the older-family-version warning has never fired on any supported Revit, because `BasicFileInfo.SavedInVersion` was removed in 2020 and the `getattr` default hid it - switched to `Format` with a pre-2020 fallback and a guard so an unreadable header claims nothing. | `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/{script.py,families_transfer_ui.py,families_transfer_state.py}`, `EasyBIM.tab/Parameters.panel/Load Parameters.pushbutton/load_parameters_revit.py`, `Development Space/tests/test_families_transfer_state.py`, `Development Space/tests/test_load_parameters_command_names.py` | `python3 -m pytest "Development Space/tests"` (986 passed); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; pyflakes clean | In Revit: tick families in all three Families Transfer cards and confirm the footer total matches; point Load Parameters at a folder holding a genuinely older `.rfa` and confirm the "will be upgraded" label and the acknowledgement tick now appear. Next: the fourth source (families from unopened `.rvt` files) and the modeless conversion. |
| 2026-08-14 | Families Transfer now shows Revit's own "Family Already Exists" prompt instead of answering it silently, via `UIDocument.GetRevitUIFamilyLoadOptions()`; a declined overwrite is reported as skipped and the run continues; "overwritten" is decided by asking the target. Rung 3's skip message now names the routes that do work. Export warns once before replacing files. Corrected `copy_paste.py` and the 2026-08-04 decision row, which both documented `DuplicateTypeAction.UseOtherTypes` - a member that does not exist in any shipped version. | `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/{families_transfer_revit.py,families_transfer_state.py,script.py}`, `lib/easybim/copy_paste.py`, `Development Space/tests/test_families_transfer_{state,command_names,links}.py`, `Development Space/tests/test_linked_sheets_copy_command_names.py`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (1005 passed); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; pyflakes clean; three mutations of the new logic each caught by exactly one test | **Not verified in Revit.** Transfer one family that already exists (Revit's dialog appears, overwrite lands, placed instances survive); a batch with **three or more** clashes - tick "Do this for all loading families" on the first dialog and record whether the rest prompt again, which is the open question; decline one (skipped, rest continue); export into a folder holding some of the names. Still open from earlier rounds: whether `link_doc.EditFamily()` succeeds or throws - it decides whether rung 3 is ever reached. |
| 2026-08-14 | Corrected the apply-to-all claim in `README.md` and the 2026-08-14 decision row: Revit's overwrite dialog does have a "Do this for all loading families" checkbox. Pinned the thing that could make it span a batch - `transfer_families` fetches the load options exactly once, outside the per-family loop - with a test that counts the fetches, replacing one that returned a shared fake and so could not catch a regression. | `README.md`, `AGENTS.md`, `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/families_transfer_revit.py`, `Development Space/tests/test_families_transfer_links.py` | `python3 -m pytest "Development Space/tests"`; `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; pyflakes clean; the new lifetime test mutation-checked by moving the fetch inside the loop | **The open question, and the point of the next Revit session:** transfer several families where three or more already exist in a target, tick "Do this for all loading families" on the first dialog, and record whether the remaining clashes prompt again. Also record whether the checkbox appears at all when the load is API-driven one family at a time. **Superseded 2026-08-14:** it does not span an API-driven batch, and per-family dialogs were rejected as too annoying - the prompt is now ours, with a checkbox that does span. |
| 2026-08-14 | Replaced Revit's native overwrite dialog with our own `TaskDialog` - command links plus a "Do this for all loading families" checkbox that actually spans the batch, because Revit's own tick does not survive a load driven one family at a time. Raised from inside `OnFamilyFound` so it asks exactly when Revit would; a byte-identical family loads silently. Cancel skips one family and ignores the tick. Declining is now read from our own flag rather than inferred from an exception message. | `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/{families_transfer_revit.py,script.py}`, `Development Space/tests/test_families_transfer_{links,command_names}.py`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (1014 passed); `compileall`; pyflakes clean; five mutations of the new logic each caught by the test that should catch it | **Not verified in Revit.** Transfer three or more families that already exist: confirm the window looks like the native one, that ticking the box silences the rest, that Cancel skips only one and the next still asks, and - the risk being taken - that a modal shown from inside `OnFamilyFound` does not upset the transaction `LoadFamily` runs underneath it. |
| 2026-08-14 | Converted Sheet Manager to a modeless window: `lib/easybim/external_events.py` bridge (first ExternalEvent) routes Select Title Blocks (silent, immediate), Apply Changes (+ link reload), Save Print Set, sheet-list/print-set loads, parameter discovery/columns and filters through `Execute`; persistent engine + stale-module drop + single instance in `script.py`; focus-return sync, Refresh button, pre-Apply stale-cell recheck (skip + report + stays red); Escape no longer closes the window; close prompt with pending edits; child dialogs owned by the window | `EasyBIM.tab/Sheet.panel/Sheet Manager.pushbutton/{script.py,sheet_manager_ui.py,sheet_manager_revit.py,sheet_manager_state.py,SheetManagerWindow.xaml,bundle.yaml}`, `lib/easybim/external_events.py`, `Development Space/tests/{test_external_events.py,test_sheet_manager_state.py,test_sheet_manager_command_names.py}`, `AGENTS.md` | `python -m pytest "Development Space/tests"` (1033 passed), `python -m compileall EasyBIM.tab lib`, pyflakes on changed modules | In-Revit: ExternalEvent handler creation from IronPython, selection repaint right after Execute, Raise while a Revit command is active, light-sync timing on a ~500-sheet model, single-instance re-activate after a pyRevit reload, doc-close -> window closes on next action |
| 2026-08-15 | Added the `Families Downgrade` bundle to `Misc Tools`: one button, two modes. **Export** picks families through the shared selection pages and writes one `<Family>.downgrade` package per family (or per type) - `manifest.json` with parameters, types, formulas, materials, subcategories, visibility, MEP connectors, symbolic/model lines and reference planes, plus one SAT (or DWG/ACIS) file per attribute group exported through a scratch 3D view with detail-level retries. **Rebuild** turns packages into native `.rfa` files of the running Revit: template ladder (hosting over category), category fallback table, the 2021/2022+ parameter ladder, types, formulas, self-checked import -> `FreeFormElement`s with attributes, connectors on matching faces, lines, reference planes, `SaveAs`. Both modes warn once before replacing, run behind a cancellable progress bar and write `families_downgrade_report.txt` next to the outputs. Moved Families Transfer's selection machinery into `lib/easybim/family_selection_*` for the second consumer | `EasyBIM.tab/Misc Tools.panel/Families Downgrade.pushbutton/*` (script, `families_downgrade_{state,revit,export,rebuild,ui}.py`, 3 XAML, bundle, both icons), `lib/easybim/family_selection_{state,ui,revit}.py`, `lib/easybim/ui/family_selection_{source,families,links}.xaml`, `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/{script.py,families_transfer_state.py,families_transfer_ui.py,families_transfer_revit.py}`, `Development Space/tests/test_families_downgrade_{state,revit,command_names}.py`, `Development Space/tests/test_families_transfer_{state,command_names,links}.py`, `Development Space/docs/superpowers/specs/2026-08-14-families-downgrade-design.md`, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (1147 passed, incl. the enum-coverage checks against the NuGet API XML/DLL on this machine); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; pyflakes clean; XAML parse + per-window handler/`x:Name` resolution for the three new windows and the three moved ones | **Nothing here is verified in Revit, and the design turns on things only Revit settles.** In order: (1) round trip in one release - export a mechanical-equipment family with two duct connectors and symbolic lines, rebuild in the same Revit, load both and compare geometry, types, formulas, connector positions, plan symbol; (2) 2026 -> 2021 for the same family and for a pipe fitting with **One family per type** - first with SAT and, if 2021 refuses the files, again with **DWG with ACIS solids**, recording which carrier works so the default can be pinned; (3) a `Visible`-associated form and a coarse-only form (visibility survives; the detail-level retry fires); (4) an opened `.rfa` source is unchanged afterwards; (5) a link family; (6) a Plumbing Equipment family lands as Plumbing Fixtures with the note; (7) cancel mid-run in each mode leaves no half package; (8) rebuild into a folder holding one of the names (one warning); (9) Rebuild with no project open in Revit 2021 (`context: zero-doc`). Also confirm SAT export from a family-document 3D view honours `HideElements`, `FreeFormElement.Create` accepts the imported bodies, and the SAT unit header (the self-check corrects 12 / 25.4 / 304.8). |
| 2026-08-15 | Added the `My Ribbon` bundle to `Misc Tools`: pick buttons from other pyRevit extensions (GitHub/git link downloaded as a normal extension, installed extensions, pyRevit's catalogue) and place them on any EasyBIM panel, a new panel, a tab of your own or another tab; hide the source's own tab; export/import the setup as JSON. Sources are ordinary installed extensions, so pyRevit ▸ Update / EasyBIM Auto Update keep them fresh; placements share the live ribbon item (the `modify_ribbon` mechanism) and are rebuilt every session from the first Idling tick. Icon drawn as SVG (kept beside the spec) and rasterised to 96x96 light/dark PNGs | `EasyBIM.tab/Misc Tools.panel/My Ribbon.pushbutton/*` (script, `my_ribbon_{state,host,ui}.py`, 6 XAML, bundle, both icons), `lib/easybim/my_ribbon.py` (new: registry + apply engine + `list_ribbon`), `lib/easybim/idling.py` (`_run_my_ribbon_apply` consumer), `startup.py`, `Development Space/tests/test_my_ribbon{,_state,_host,_command_names}.py`, `test_idling_dispatcher.py`, `Development Space/docs/superpowers/specs/2026-08-15-my-ribbon-design.md` + icon SVGs, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (1150 passed); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; pyflakes clean; XAML parse + per-window handler/`x:Name` resolution + star-row `MinHeight` checks | **Nothing verified in Revit yet.** In order of risk: (1) paste a public repository whose root is an extension - it lands under `%APPDATA%\pyRevit\Extensions`, reload, its tab hides, the picked buttons sit on a new EasyBIM-tab panel and on a new tab with icons/tooltips, and a click runs the tool; (2) pyRevit ▸ Update pulls a change there and Reload keeps the placements; EasyBIM Auto Update at startup does the same; (3) a context-limited tool greys out on the copy; (4) a whole pull-down and a single pull-down child as a top-level button (fallback: whole pull-down); (5) an EasyBIM button on a custom tab; on Misc Tools itself it must not duplicate; (6) uninstall a source through pyRevit's Extensions window - manager shows missing, no crash; Remove deletes only folders My Ribbon installed; (7) export on A, import on B - same ribbon after one reload; (8) cancel a download half-way - nothing left; (9) private repo with token; monorepo root registration and its update through pyRevit ▸ Update; (10) hidden-tab flash after reload is brief; created tabs/panels vanish after Remove + Apply; (11) which LibGit2Sharp pyRevit ships - `CloneOptions.OnTransferProgress`/`CredentialsProvider` directly (0.26) or under `FetchOptions` (0.27+): the code tries both, but if neither takes, progress/Cancel/sign-in are silently dropped, so watch the progress bar move; (12) a shared item still renders and enables while its home tab is `IsVisible = False`, and an item added to a native Revit panel behaves |
| 2026-08-16 | My Ribbon round 2: **Show/Hide tabs...** window (EasyBIM + Modify frozen, pyRevit hideable with a note) over a new `hidden_tabs` registry list, Apply now closes the window; **Uninstall** next to Remove (Remove keeps files); the Installed card lists **Revit's own tabs and other add-ins** read live from the ribbon, their buttons placeable like any other; a fourth **Dynamo** card turns `.dyn` files into real pyRevit bundles in My Ribbon's own `EasyBIM_MyRibbon.extension` (run the original via `dynamo_path`, copy as fallback, Revit's own Dynamo icon copied at apply, custom PNG, Locate... for moved graphs). Drawn Dynamo fallback icon as SVG beside the spec + 96x96 PNGs in the bundle | `lib/easybim/my_ribbon.py` (hidden_tabs, PROTECTED_TABS, `describe_ribbon_tab`/`describe_tab_by_title`, `find_native_dynamo_button`, `_apply_dynamo_icons`, `list_ribbon` flags), `EasyBIM.tab/Misc Tools.panel/My Ribbon.pushbutton/*` (script, `my_ribbon_{state,host,ui}.py`, `MyRibbonWindow.xaml`, `SourceSelectionWindow.xaml`, new `TabVisibilityWindow.xaml`, `DynamoButtonWindow.xaml`, `dynamo_icon*.png`), `Development Space/tests/test_my_ribbon{,_state,_host,_command_names}.py`, spec `2026-08-16-my-ribbon-tabs-uninstall-native-dynamo-design.md` + icon SVGs, `README.md`, `AGENTS.md` | `python3 -m pytest "Development Space/tests"` (1192 passed); `compileall`; pyflakes clean; XAML parse + handler/`x:Name` + star-row `MinHeight` checks | **Not verified in Revit.** (1) Show/Hide: hide an add-in tab and the pyRevit tab, Apply -> gone; EasyBIM/Modify cannot be unticked; un-hide brings them back; (2) Uninstall a downloaded repo -> folder gone after Apply + reload; Remove keeps files; (3) place a native button (Annotate > Tag by Category) and an add-in button on your tab - click works, greys out as at home; a whole split button; (4) add a .dyn -> reload -> button with Revit's Dynamo icon; click runs; edit the graph on disk -> next click runs the new one; Ctrl+click opens Dynamo; custom PNG shows; move the graph -> source missing, Locate fixes it; (5) Apply closes the window; (6) export/import across machines with a native tab, an absent add-in tab, a Dynamo path that does not exist |
| 2026-08-19 | Added the `Family Types` bundle to `Misc Tools`: the family's whole type table as an editable modal grid (types as rows, parameters as columns, instance parameters marked `(default)`), with add/rename/delete of types, staged red edits, Excel export whose header cells carry the parameter name over its ElementId, and an import that opens a type-catalogue-style **Specify Types** dialog and stages what is ticked. Runs on the family in the editor or on a family picked from a project, reloading it through the shared *Family Already Exists* prompt. Promoted that prompt and `FamilyLoadOptions` out of Families Transfer into `lib/easybim/family_load_options.py` for this second consumer | `EasyBIM.tab/Misc Tools.panel/Family Types.pushbutton/*` (script, `family_types_{state,revit,ui,xlsx}.py`, 2 XAML, bundle, both icons), `lib/easybim/family_load_options.py`, `EasyBIM.tab/Misc Tools.panel/Families Transfer.pushbutton/{script.py,families_transfer_revit.py}`, `Development Space/tests/test_family_types_{state,xlsx,command_names}.py`, `Development Space/tests/test_families_transfer_{command_names,links}.py`, `Development Space/docs/superpowers/specs/2026-08-19-family-types-design.md`, `README.md`, `AGENTS.md` | `python3 -m unittest discover` over `Development Space/tests` (1347 passed, 4 pre-existing skips for absent Revit assemblies); `python3 -m compileall "EasyBIM.tab" lib`; XAML parse + per-window handler/`x:Name` resolution for both new windows; the xlsx test writes a real workbook and reads it back through `easybim.excel_workbook` | **Nothing verified in Revit.** In order: (1) family editor - open an `.rfa` with several types, confirm every type is a row and every parameter a column, instance ones marked `(default)`, formula cells greyed with the formula in the tooltip; edit a length, a yes/no, a text and a material, add a type, rename one, delete one, Apply, then compare against Revit's own Family Types dialog and confirm Ctrl+Z undoes the lot; (2) project - pick the same family, edit, Apply, confirm the *Family Already Exists* dialog appears, that *overwrite parameter values* changes the project's type values while plain *overwrite* leaves them, that Cancel loads nothing and still closes the family document, and that a family already open in the editor survives; (3) Excel - export and confirm each header cell really shows name over id (the `text_wrap` claim), instance headers blue, read-only headers red with grey locked cells; edit values, add a row, delete a row, import and check the Specify Types statuses, that unticking excludes, and that the delete checkbox marks rather than writes; (4) edges - a one-type family (delete refused), a family with no named types (single `(default)` row), a duplicate name typed in the grid (Apply blocked), a material name not in the family (reported), and a cleared numeric cell (reported as skipped). Also confirm the guarded probes bind: `FamilyParameter.Id`, `FamilyType.AsValueString`, `FamilyManager.SetValueString`, `RenameCurrentType`, `DeleteCurrentType` |
| 2026-08-19 | `Auto Update` and `My Ribbon` get `context: zero-doc` so both stay clickable with no model open. Neither command reads the document, and both are most useful on Revit's start page: Auto Update refreshes the extensions before any model is loaded, and My Ribbon rebuilds the ribbon that the rest of the session uses. pyRevit's default availability requires an active document, which is why they greyed out | `EasyBIM.tab/Misc Tools.panel/Auto Update.pushbutton/bundle.yaml`, `EasyBIM.tab/Misc Tools.panel/My Ribbon.pushbutton/bundle.yaml`, `Development Space/tests/test_auto_update.py`, `Development Space/tests/test_my_ribbon_command_names.py`, `AGENTS.md` | `python3 -m unittest discover -s "Development Space/tests"` (1311 passed, 5 skipped); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py` | **Not verified in Revit.** Start Revit with no model open and confirm both buttons are enabled on the start page, that Auto Update runs its check and reload from there, and that My Ribbon opens, applies and reloads without an active document |
| 2026-08-19 | Reshaped the EasyBIM tab. New `Family.panel` collects `Family Types`, `Families Downgrade` and `Families Transfer` out of `Misc Tools`, with a `layout` so the alphabet does not push `Family Types` behind the two `Families` buttons. `Grid.panel` deleted and its lone `Grid Offset` moved into `Misc Tools`. `My Ribbon` moved to a panel of its own: pyRevit has no tab-level button, so a panel holding one button is as close to a single button as the ribbon gets, and the panel keeps its label. Panel order stays alphabetical (no tab-level `bundle.yaml`), so the tab now reads Family, Keynote, Links, Message, Misc Tools, My Ribbon, Parameters, Sheet, Views. Command folders moved with `git mv`, so no script, XAML or icon changed; `lib/easybim/my_ribbon.py` and `modify_ribbon.py` read panels off the live ribbon and needed no edit | `EasyBIM.tab/Family.panel/*` (moved bundles + new `bundle.yaml`), `EasyBIM.tab/My Ribbon.panel/My Ribbon.pushbutton/*` (moved), `EasyBIM.tab/Misc Tools.panel/Grid Offset.pushbutton/*` (moved), removed `EasyBIM.tab/Grid.panel`, `Development Space/tests/test_ribbon_panel_layout.py` (new), `Development Space/tests/test_{families_downgrade_*,families_transfer_*,family_types_*,my_ribbon_*}.py` (panel paths), `README.md`, `AGENTS.md` | `python3 -m unittest discover -s "Development Space/tests"` (1403 passed, 17 skipped); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; the new layout tests mutation-checked by reordering the `Family` layout and by moving `Grid Offset` to the wrong panel (3 failures) | **Not verified in Revit.** Reload pyRevit and confirm: the `Family` panel shows the three buttons in the pinned order with both icon themes; `Grid Offset` sits in `Misc Tools` and still runs; `My Ribbon` has its own panel and its placements survive (a My Ribbon destination saved as `Misc Tools` still resolves; a placement of a moved EasyBIM button lands on the new panel, and an EasyBIM button placed on its own home panel must still not duplicate); the Modify-tab shortcuts (`Slope`, `3D Rotate Multiple`, `Flip Multiple`) still mirror |
| 2026-08-19 | Renamed the one-button `My Ribbon.panel` to `General.panel`, keeping `My Ribbon` as its only command for now - a named home that other cross-cutting buttons can join, rather than a panel whose label repeats the button under it. Panel order stays alphabetical, so the tab now reads Family, General, Keynote, Links, Message, Misc Tools, Parameters, Sheet, Views | `EasyBIM.tab/General.panel/My Ribbon.pushbutton/*` (moved from `My Ribbon.panel`), `Development Space/tests/test_ribbon_panel_layout.py`, `Development Space/tests/test_my_ribbon_{command_names,host,state}.py` (panel paths), `README.md`, `AGENTS.md` | `python3 -m unittest discover -s "Development Space/tests"` (1404 passed, 17 skipped); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py` | **Not verified in Revit.** Reload pyRevit and confirm the `General` panel sits second on the tab with the `My Ribbon` button and both icon themes, that clicking it opens the window, and that existing My Ribbon placements and destinations still resolve |
| 2026-08-19 | Renamed the `Linked Sheets Copy` command to `Linked Sheets Transfer`, all the way down: bundle folder, two-line ribbon title, `Sheet.panel` layout entry, the three sibling modules (`linked_sheets_transfer_{state,revit,ui}.py`), `LinkedSheetsTransferWindow.xaml` and its class, the `lst_revit` alias, and every user-facing string (`__title__`, `TITLE`, the three window titles, the progress bar, the failure alert). The verb `copy` stays wherever it names the operation - `run_copy`, `copy_parameters`, the "Copy title block" options - because the tool still copies sheets out of a link; only the command's name changed. Added `RenameSurvivedTests`: `script.py` reaches its modules, window classes and XAML by name at runtime, so a half-done rename imports, compiles and passes every other test, then dies on the first click | `EasyBIM.tab/Sheet.panel/Linked Sheets Transfer.pushbutton/*` (renamed from `Linked Sheets Copy.pushbutton`), `EasyBIM.tab/Sheet.panel/bundle.yaml`, `EasyBIM.tab/Views.panel/View Align.pushbutton/script.py`, `lib/easybim/sheet_geometry.py`, `Development Space/tests/test_linked_sheets_transfer_{command_names,state}.py` (renamed), `test_sheet_geometry.py`, `test_families_transfer_command_names.py`, `README.md`, `AGENTS.md` | `python3 -m unittest discover -s "Development Space/tests"` (1409 passed, 17 skipped); `python3 -m compileall "EasyBIM.tab" lib hooks startup.py`; the new tests mutation-checked with three half-done renames (stale XAML constant, stale window class, stale module import), each caught | **Not verified in Revit.** Reload pyRevit and confirm the `Sheet` panel shows `Linked Sheets Transfer` second with both icon themes, then run it end to end on a model with a loaded link: the main window, the confirm dialog, the progress bar and the report window must all open and read `Linked Sheets Transfer`. A pyRevit reload is required - the old bundle folder no longer exists, so a stale session still points at it. Any My Ribbon placement of the old button will report missing and needs re-picking |
| 2026-08-19 | Family Types round 2, from the first run in Revit: **fixed the over-locking** (dropped the `IsReadOnly` / `UserModifiable` gates - see the correction row), **yes/no parameters are now checkboxes** bound to a bool, **cell multi-select bulk edit** (drag/shift-select down a column, edit one, all follow; plus a `Fill Down` button and the `PreviewMouseLeftButtonDown` guard), and a **type/instance band** above the column headers | `EasyBIM.tab/Family.panel/Family Types.pushbutton/{family_types_state.py,family_types_revit.py,family_types_ui.py,FamilyTypesWindow.xaml}`, `Development Space/tests/test_family_types_{state,xlsx,command_names}.py`, `README.md`, `AGENTS.md` | `python3 -m unittest discover` over `Development Space/tests` (1422 passed, 4 pre-existing skips); `python3 -m compileall "EasyBIM.tab" lib`; pyflakes clean; a static check that all 23 `System.*` imports resolve to the right namespace and are used | **Only the over-locking diagnosis is certain; the rest is unverified in Revit.** In order: (1) confirm the columns that were wrongly grey are editable now, and that what stays grey names a real reason in its tooltip - if anything is still over-locked, the tooltip says which rule did it; (2) yes/no cells render and toggle as checkboxes; (3) drag down a column, type, Enter - every selected cell takes it; same for a checkbox; same via Fill Down; (4) the band lines up over the right columns and stays aligned while scrolling horizontally, after Hide Empty Columns, and after a column drag - it hides itself if it cannot, so a missing band means the sync failed rather than the grid breaking; (5) row headers still select whole rows for the type buttons; (6) an export still opens and re-imports as Unchanged. |
| 2026-08-19 | Family Types round 3: Yes/No parameters were still showing as 0 and 1, so detection was rebuilt on four independent routes (spec object, spec id text, legacy enum, and a value-string fallback that needs no spec knowledge), `_data_spec` was factored out of the three places that duplicated it, and the Revit layer got its first tests | `EasyBIM.tab/Family.panel/Family Types.pushbutton/family_types_revit.py`, `Development Space/tests/test_family_types_revit.py` (new), `AGENTS.md` | `python3 -m unittest discover` over `Development Space/tests` (1441 passed, 4 pre-existing skips); `compileall`; pyflakes clean; **mutation-checked** - restoring the shipped detection fails exactly the two fallback tests, so they are not vacuous | **The spec routes still cannot be verified off Revit - the fallback is what makes this safe.** Confirm Yes/No parameters now render as checkboxes and toggle. If any still show 0/1, that parameter's value string carries a digit or it has no value on any type: export to Excel and send the `_metadata` sheet's `StorageType` and `SpecLabel` columns, which name exactly what this Revit reported. Also re-confirm nothing that is genuinely a number (a count, a parameter rendering as "1 mm") became a checkbox. |
| 2026-08-21 | **Correction.** `FilteredElementCollector(doc, view_id)` is scoped by *visibility*, never by ownership; `OwnerViewId` is the ownership test | The Align Title Block sub-option shipped reading a sheet's contents with a bare view-scoped collector and moving everything it returned. That collector also returns the model elements seen *through* the placed viewports, so `ElementTransformUtils.MoveElement` translated real model geometry by a **paper-space** vector - the drawings then appeared misaligned because the model had moved. The repo already knew this three times over and the new code was the one place that did not: `families_downgrade_export._visible_element_ids` names the same call "everything visible in it"; `Grid Offset` depends on the behaviour to reach project-wide `DB.Grid` datums through a view; and `sheet_manager_revit.copy_sheet_detailing` plus `linked_sheets_transfer_revit.sheet_detailing_ids` both guard it with `eid_to_int(element.OwnerViewId) != sheet_id -> continue` | The rule now lives in `sheet_titleblocks.sheet_owned_ids`, pure and mutation-checked: restoring the permissive version fails six tests. An unreadable owner **excludes**, as in both precedents. A viewport is admitted by its own flag and never by `OwnerViewId`, because both precedents drop viewports *before* their owner test, so `Viewport.OwnerViewId` is unproven here. A title-block revision schedule is dropped - it rides on the title block and would shift twice (`sheet_manager_revit` guards it the same way). **The lesson: a docstring saying "owns" over a call that means "sees" is the bug.** The old test fake returned only what a test seeded for the sheet, so it rubber-stamped the false premise; it now models the real collector | Active |
| 2026-08-21 | `View Align` now measures each viewport **last** - after every other write and a `doc.Regenerate()` - and places it through `SheetProjection.box_center_for` instead of open-coding the arithmetic. This retires the "can misplace a viewport" caveat the 2026-08-04 row pins on it | The apply step mixed a **stale snapshot with a fresh read**: `target_anchor` was solved in the precheck, freezing `View.Outline` and `Viewport.GetBoxCenter()` before the transaction opened, and then the placement re-read `GetBoxCenter()` fresh and added a delta derived from the frozen pair. The residual is exactly `(C_now - C_pre) + (L_pre - L_true)` - two independent staleness channels. `sheet_geometry`'s own docstring already said a projection "is only valid for as long as the view is untouched. Build it *after* every configuration write, never before", and `box_center_for` already existed to avoid the mix by using `self.box_center` on both sides - only `Linked Sheets Transfer` was calling it | The latent defect surfaced as a bug report only once `Move other sheet elements with it` put N writes ahead of the measurement instead of one, which is the tell: **an option did not introduce the defect, it widened the window on one that was already there.** Apply is now three phases - configure, regenerate, measure-and-place - and each projection is built inside its own iteration, immediately before its own `SetBoxCenter`, so a previous target's write cannot stale it (moving a box does not resize it). The precheck still solves an anchor, but purely to prove a viewport is measurable; the value is deliberately not carried forward. A temporary diagnostics block printed every shift and box centre while the fix was confirmed against a real model - a misplaced frame is only diagnosable by whether its displacement is an exact multiple of the shift or an arbitrary residual - and was removed once confirmed. Also fixed alongside: extra title-block instances were moved only when the sub-option was on, since `_sheet_owned_elements` lacked the title-block category filter both sibling tools apply | Active |
| 2026-08-21 | `View Align` walks the host document **once** at startup: `_load_target_rows` seeds `_reference_rows_by_doc_key`, and the candidate scan takes no model point | Two caches held the same data and neither knew about the other - `_load_target_rows` stored rows in `_target_rows`, then `SelectedIndex = 0` on the document combo fired `_refresh_reference_sheet_combo`, which missed the empty `_reference_rows_by_doc_key` and walked every viewport in the project a second time, with the same document and the same arguments, before the window could open. Inside that walk the view was resolved twice and `_is_supported_view_type` run twice per viewport, a projection was built and its anchor discarded, and `SheetNumber`/`Name` were re-read once per viewport instead of once per sheet | Measured on the real code against fake Revit objects, 120 sheets x 8 viewports: rows byte-identical before and after, interop reads per scan 15,600 -> 12,000, and startup goes from two walks to one - 31,200 -> 12,000 reads, 62% less. `_collect_candidate_viewport_rows` lost its `model_point` argument because measurability is a property of the view and the viewport, not of any point: the old filter only ever asked whether a projection could be built, since `to_xyz(XYZ.Zero)` cannot fail. **The lesson: a second cache for data you already hold is a second scan, and `SelectionChanged` firing from `__init__` is where it hides.** Deferred tree loading was considered and deliberately not done - the window still opens fully populated | Active |
| 2026-08-21 | `View Align`'s target tree draws only the rows on screen, and its checkbox handler moved from the row template to the tree - the shape Sheet Manager and 13 other files here already use | Closing was slower than opening. `IsExpanded=True` was forced on every `TreeViewItem` with no virtualization, so ~1,080 rows were realized at once - roughly 21,600 visual elements, 5,400 bindings and, because `Click` sat *inside* the `HierarchicalDataTemplate`, 1,080 handler registrations - all of which had to be unwound at close. Building is spread over the open; freeing happens in one burst when the script's scope exits | Follows `circuit_schedule_panel.xaml`, the repo's only other virtualized TreeView: `ScrollViewer.CanContentScroll="True"` ships alongside `IsVirtualizing` because a TreeView defaults it to `False` and virtualization does not engage without it. Expansion moved onto the data (`TargetViewportNode.is_expanded`, bound TwoWay) because `ContainerFromItem` returns null for an unrealized row and a recycled container is re-bound to another node; the window keeps `_collapsed_sheet_ids` since `_refresh_target_tree` builds new nodes each time. Deleting `_set_container_expanded_recursive` also removed 1,080 nested `UpdateLayout()` calls. **A harness driving the real methods against fakes caught Collapse All doing nothing** - `_refresh_target_tree`'s capture pass overwrote the state Expand/Collapse had just set, which is why it now takes `capture_expansion`. **The lesson: a silent no-op is what a UI perf change fails as, so prove the affected behaviour rather than the timing.** | Active |
| 2026-08-21 | Restoring a pin in `View Align` is reported and never fatal: both sites go through one `_restore_pins(pinned, stats)`, and `View Align` finally has a test file | The two re-pin loops were broken in **opposite** directions. The title-block one caught every failure with a bare `except Exception: pass`, so an element was left unlocked in a committed transaction with nobody told; the viewport one had no guard at all, so a refused pin threw into `run_click`'s handler and rolled back an alignment that had already succeeded. Re-pinning is housekeeping after the real work: silence and rollback are both wrong answers | Failures go to `stats.add_note` - the non-blocking channel, since `stats.issues` aborts before apply and we are past that - and the note names the sheet, plus the view for a viewport, so the `Pinned unpinned`/`Pinned restored` counters only disagree where a note explains why. New `test_view_align_window.py` is the tool's first coverage: it loads the pushbutton `script.py` behind stubs and pins the pin contract, tree expansion (Collapse All reaching unrealized rows) and the tree-level checkbox handler ignoring expander clicks. Mutation-checked - the old swallow fails 2 tests, the old raise fails 5. **The loader restores `sys.modules` in a `finally`: the first version leaked `pyrevit`/`clr`/`System` stubs and broke `test_temp_phase_close_runtime`, whichever ran first. A test double that outlives its test is another test's bug.** | Active |
