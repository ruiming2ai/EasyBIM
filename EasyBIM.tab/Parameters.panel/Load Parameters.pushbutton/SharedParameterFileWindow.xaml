<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Load Parameters - Shared Parameter File"
        Width="640"
        Height="640"
        MinWidth="520"
        MinHeight="440"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock x:Name="FileText"
                       FontSize="15"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap" />
            <TextBlock Foreground="DimGray"
                       TextWrapping="Wrap"
                       Margin="0,2,0,0"
                       Text="Tick the parameters to add to the list. They are grouped exactly as the file groups them." />
        </StackPanel>

        <TextBox x:Name="SearchBox"
                 Grid.Row="1"
                 Height="26"
                 Padding="6,2,6,2"
                 VerticalContentAlignment="Center"
                 Margin="0,0,0,8"
                 ToolTip="Filter by parameter name or data type"
                 TextChanged="search_changed" />

        <Border Grid.Row="2"
                BorderBrush="#B8B8B8"
                BorderThickness="1"
                Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="GroupPanel" />
            </ScrollViewer>
        </Border>

        <DockPanel Grid.Row="3" LastChildFill="False" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Select All"
                        Width="90"
                        Height="30"
                        Margin="0,0,6,0"
                        Click="select_all_click" />
                <Button Content="Select None"
                        Width="90"
                        Height="30"
                        Margin="0,0,6,0"
                        Click="select_none_click" />
                <TextBlock x:Name="CountText"
                           Foreground="DimGray"
                           VerticalAlignment="Center"
                           Margin="6,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button x:Name="AddButton"
                        Content="Add"
                        Width="100"
                        Height="30"
                        Margin="0,0,6,0"
                        IsDefault="True"
                        Click="add_click" />
                <Button Content="Cancel"
                        Width="100"
                        Height="30"
                        IsCancel="True"
                        Click="cancel_click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
