<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selective Transfer"
        Height="640"
        Width="560"
        MinHeight="480"
        MinWidth="480"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Closing="window_closing">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="source_name_tb"
                       FontSize="14"
                       FontWeight="SemiBold"/>
            <TextBlock Text="All settings start unchecked. A V/G Overrides checkbox toggles every item in its Edit window; open Edit to pick individual items."
                       Margin="0,6,0,0"
                       FontSize="12"
                       Foreground="DimGray"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <DataGrid x:Name="params_dg"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="#D0D0D0"
                  VerticalGridLinesBrush="#D0D0D0"
                  BorderBrush="#8F8F8F"
                  BorderThickness="1"
                  Background="White"
                  RowHeaderWidth="0"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="Include"
                                       HorizontalAlignment="Center"
                                       FontWeight="SemiBold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsThreeState="False"
                                      IsChecked="{Binding include_checked, Mode=OneWay}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="*">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="Parameter"
                                       HorizontalAlignment="Center"
                                       FontWeight="SemiBold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding name}"
                                       Margin="6,0,4,0"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="80">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="Edit"
                                       HorizontalAlignment="Center"
                                       FontWeight="SemiBold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="edit_btn"
                                    Content="Edit..."
                                    Height="22"
                                    Margin="6,2,6,2"
                                    Visibility="Collapsed"/>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding has_edit}" Value="True">
                                    <Setter TargetName="edit_btn" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="2" Margin="0,10,0,0">
            <TextBlock x:Name="selective_status_tb"
                       DockPanel.Dock="Left"
                       VerticalAlignment="Center"
                       Foreground="DimGray"/>
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="transfer_btn"
                        Content="Transfer"
                        Width="110"
                        Height="32"
                        Margin="0,0,8,0"
                        IsDefault="True"
                        Click="transfer_click"/>
                <Button Content="Cancel"
                        Width="110"
                        Height="32"
                        IsCancel="True"
                        Click="cancel_click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
