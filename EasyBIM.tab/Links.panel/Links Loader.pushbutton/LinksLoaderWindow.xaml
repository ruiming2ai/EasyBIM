<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Links Loader"
        Height="480"
        Width="780"
        MinHeight="360"
        MinWidth="600"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="LinksGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#dedede"/>
            <Setter Property="VerticalGridLinesBrush" Value="#dedede"/>
            <Setter Property="EnableColumnVirtualization" Value="True"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode"
                    Value="Recycling"/>
        </Style>
    </Window.Resources>
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Links Loader"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4"/>

        <TextBlock x:Name="subtitle_tb"
                   Grid.Row="1"
                   Foreground="#666666"
                   Margin="0,0,0,10"/>

        <DataGrid x:Name="links_dg"
                  Grid.Row="2"
                  Style="{StaticResource LinksGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding name}"
                                    Width="200"/>
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding element_type}"
                                    Width="110"/>
                <DataGridTextColumn Header="Path"
                                    Binding="{Binding path}"
                                    Width="*"/>
                <DataGridTextColumn Header="Status"
                                    Binding="{Binding status}"
                                    Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button x:Name="export_b"
                    Content="Export"
                    Width="120"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="export_click"/>
            <Button x:Name="import_b"
                    Content="Import"
                    Width="120"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="import_click"/>
            <Button Content="Close"
                    Width="100"
                    Height="32"
                    IsCancel="True"
                    Click="close_click"/>
        </StackPanel>
    </Grid>
</Window>
