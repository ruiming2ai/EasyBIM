<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Link Paths"
        Height="520"
        Width="880"
        MinHeight="400"
        MinWidth="700"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="PreviewGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#dedede"/>
            <Setter Property="VerticalGridLinesBrush" Value="#dedede"/>
            <Setter Property="EnableColumnVirtualization" Value="True"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode"
                    Value="Recycling"/>
        </Style>
    </Window.Resources>
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Import Preview"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4"/>

        <TextBlock x:Name="source_tb"
                   Grid.Row="1"
                   Foreground="#666666"
                   TextTrimming="CharacterEllipsis"
                   Margin="0,0,0,10"/>

        <DataGrid x:Name="plan_dg"
                  Grid.Row="2"
                  Style="{StaticResource PreviewGridStyle}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header=""
                                        Binding="{Binding is_selected}"
                                        Width="30"
                                        IsReadOnly="False"/>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding name}"
                                    Width="170"/>
                <DataGridTextColumn Header="Current Path"
                                    Binding="{Binding old_path}"
                                    Width="*"/>
                <DataGridTextColumn Header="New Path"
                                    Binding="{Binding new_path}"
                                    Width="*"/>
                <DataGridTextColumn Header="Status"
                                    Binding="{Binding status_label}"
                                    Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="count_tb"
                   Grid.Row="3"
                   Margin="0,8,0,0"
                   Foreground="#444444"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="apply_b"
                    Content="Apply"
                    Width="120"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="apply_click"/>
            <Button Content="Cancel"
                    Width="100"
                    Height="32"
                    IsCancel="True"
                    Click="cancel_click"/>
        </StackPanel>
    </Grid>
</Window>
