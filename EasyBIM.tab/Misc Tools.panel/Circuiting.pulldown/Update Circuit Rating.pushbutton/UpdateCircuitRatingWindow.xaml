<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Circuit Rating"
        Height="640"
        Width="980"
        MinHeight="420"
        MinWidth="720"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Transfers the highest amp rating found on a circuit's elements into the circuit's own rating parameters. Circuits where no circuited element carries a value for the parameter cannot be updated and are not listed."
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="Current parameter on circuited elements:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>
            <ComboBox x:Name="SourceParamCombo"
                      Grid.Column="1"
                      Height="26"
                      SelectionChanged="source_param_changed"/>
        </Grid>

        <GroupBox Grid.Row="2" Header="Target circuit parameters" Margin="0,0,0,10">
            <ListBox x:Name="TargetParamList"
                     MaxHeight="112"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding label}"
                                  Margin="2"
                                  IsChecked="{Binding is_selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <DataGrid x:Name="CircuitsGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  RowHeaderWidth="0"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="False"
                  HorizontalGridLinesBrush="#dedede"
                  VerticalGridLinesBrush="#dedede"
                  EnableColumnVirtualization="True"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="60">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="Update"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding is_selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Panel"
                                    Binding="{Binding panel}"
                                    Width="120"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Circuit"
                                    Binding="{Binding circuit_number}"
                                    Width="80"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Load Name"
                                    Binding="{Binding load_name}"
                                    Width="170"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="From Element"
                                    Binding="{Binding donor_label}"
                                    Width="*"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="New Value"
                                    Binding="{Binding new_text}"
                                    Width="90"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Existing"
                                    Binding="{Binding existing_text}"
                                    Width="180"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Status"
                                    Binding="{Binding status}"
                                    Width="90"
                                    IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="4" Margin="0,10,0,0" LastChildFill="False">
            <Button x:Name="AllButton"
                    Content="All"
                    Height="28"
                    MinWidth="90"
                    DockPanel.Dock="Left"
                    Click="all_clicked"/>
            <Button x:Name="NoneButton"
                    Content="None"
                    Height="28"
                    MinWidth="90"
                    Margin="8,0,0,0"
                    DockPanel.Dock="Left"
                    Click="none_clicked"/>
            <TextBlock x:Name="CountText"
                       DockPanel.Dock="Left"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"
                       TextWrapping="Wrap"/>
            <Button x:Name="UpdateButton"
                    Content="Update"
                    IsDefault="True"
                    Height="28"
                    MinWidth="90"
                    DockPanel.Dock="Right"
                    Click="update_clicked"/>
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    IsCancel="True"
                    Height="28"
                    MinWidth="90"
                    Margin="0,0,8,0"
                    DockPanel.Dock="Right"
                    Click="cancel_clicked"/>
        </DockPanel>
    </Grid>
</Window>
