<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter"
        Width="560"
        Height="580"
        MinWidth="480"
        MinHeight="440"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Keep only the ticked categories, families and types. Like Revit's Filter, this only narrows the current selection."
                   Foreground="DimGray"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="CheckAllButton"
                    Content="Check All"
                    Width="100"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="check_all_click" />
            <Button x:Name="CheckNoneButton"
                    Content="Check None"
                    Width="100"
                    Height="30"
                    Click="check_none_click" />
        </StackPanel>

        <Border Grid.Row="2"
                BorderBrush="#D0D0D0"
                BorderThickness="1">
            <TreeView x:Name="FilterTree"
                      BorderThickness="0" />
        </Border>

        <TextBlock x:Name="CountText"
                   Grid.Row="3"
                   Margin="0,10,0,0"
                   FontWeight="SemiBold" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="OkButton"
                    Content="OK"
                    Width="100"
                    Height="34"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="ok_click" />
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="100"
                    Height="34"
                    IsCancel="True"
                    Click="cancel_click" />
        </StackPanel>
    </Grid>
</Window>
